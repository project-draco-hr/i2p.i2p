{
  if (className != null)   android.util.Log.println(toAndroidLevel(priority),className,threadName + ' ' + msg+ msg+ ' '+ t.toString()+ ' '+ android.util.Log.getStackTraceString(t));
 else   if (name != null)   android.util.Log.println(toAndroidLevel(priority),name,threadName + ' ' + msg+ msg+ ' '+ t.toString()+ ' '+ android.util.Log.getStackTraceString(t));
 else   android.util.Log.println(toAndroidLevel(priority),threadName,msg + ' ' + t.toString()+ ' '+ android.util.Log.getStackTraceString(t));
}
