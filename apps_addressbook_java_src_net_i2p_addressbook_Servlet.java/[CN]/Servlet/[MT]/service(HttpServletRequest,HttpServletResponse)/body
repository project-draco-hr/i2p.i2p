{
  if (this.thread != null && request.getParameter("wakeup") != null && this.nonce != null && this.nonce.equals(request.getParameter("nonce"))) {
    this.thread.interrupt();
  }
 else {
    PrintWriter out=response.getWriter();
    out.write("I2P addressbook OK");
  }
}
