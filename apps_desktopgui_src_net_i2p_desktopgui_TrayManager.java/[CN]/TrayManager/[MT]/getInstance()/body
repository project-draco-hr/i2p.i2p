{
  if (instance == null) {
    boolean inI2P=RouterManager.inI2P();
    if (inI2P) {
      instance=new InternalTrayManager();
    }
 else {
      instance=new ExternalTrayManager();
    }
  }
  return instance;
}
