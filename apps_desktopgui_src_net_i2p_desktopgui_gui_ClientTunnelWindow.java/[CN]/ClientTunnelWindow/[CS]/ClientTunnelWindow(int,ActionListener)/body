{
  initComponents();
  this.tunnelNumber=tunnelNumber;
  this.al=al;
  extraInitComponents();
  this.setDefaultCloseOperation(DISPOSE_ON_CLOSE);
  this.setSize(600,600);
  this.setLocationRelativeTo(null);
  this.requestFocus();
  this.changeTunnelState.setVisible(false);
  this.setVisible(true);
}
