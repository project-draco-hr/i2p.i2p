{
  super.add(packetToStore);
  File[] finishedPacketFiles=getAllMatchingFiles(packetToStore.getMessageId());
  if (finishedPacketFiles.length == packetToStore.getNumFragments())   assemble(finishedPacketFiles);
}
