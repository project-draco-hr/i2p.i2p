{
  byte[] randomHash=new byte[Hash.HASH_LENGTH];
  for (int i=0; i < Hash.HASH_LENGTH; i++)   randomHash[i]=(byte)RandomSource.getInstance().nextInt(256);
  Hash key=new Hash(randomHash);
  getClosestPeers(key,KademliaConstants.K);
}
