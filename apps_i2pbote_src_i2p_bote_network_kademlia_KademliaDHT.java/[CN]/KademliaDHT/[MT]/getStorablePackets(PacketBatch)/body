{
  ConcurrentHashSet<DhtStorablePacket> storablePackets=new ConcurrentHashSet<DhtStorablePacket>();
  for (  I2PBotePacket packet : batch.getResponsePackets())   if (packet instanceof DhtStorablePacket)   storablePackets.add((DhtStorablePacket)packet);
  return storablePackets;
}
