{
  String str=super.toString() + " fragIdx=" + fragmentIndex+ " numFrags="+ numFragments;
  if (content == null)   str+=" content=null";
 else   str+=" content length=" + content.length + " bytes";
  return str;
}
