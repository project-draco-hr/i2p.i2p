{
  RandomSource randomSource=RandomSource.getInstance();
  byte[] sessionId=new byte[SESSION_ID_LENGTH];
  for (int i=0; i < sessionId.length; i++)   sessionId[i]=(byte)randomSource.nextInt(256);
  return sessionId;
}
