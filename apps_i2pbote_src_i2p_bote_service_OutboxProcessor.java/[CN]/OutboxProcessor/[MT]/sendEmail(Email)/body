{
  email.saveChanges();
  email.scrubHeaders();
  for (  Address recipient : email.getAllRecipients())   if (recipient instanceof InternetAddress) {
    String recipientAddress=((InternetAddress)recipient).getAddress();
    sendToOne(recipientAddress,email);
  }
 else   log.error("Illegal recipient type: " + recipient.getType());
}
