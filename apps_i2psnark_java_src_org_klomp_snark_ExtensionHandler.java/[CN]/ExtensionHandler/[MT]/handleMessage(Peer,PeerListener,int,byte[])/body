{
  if (_log.shouldLog(Log.INFO))   _log.info("Got extension msg " + id + " length "+ bs.length+ " from "+ peer);
  if (id == ID_HANDSHAKE)   handleHandshake(peer,listener,bs);
 else   if (id == ID_METADATA)   handleMetadata(peer,listener,bs);
 else   if (id == ID_PEX)   handlePEX(peer,listener,bs);
 else   if (_log.shouldLog(Log.INFO))   _log.info("Unknown extension msg " + id + " from "+ peer);
}
