{
  if (_dht != null) {
    _dht.stop();
    _dht=null;
  }
  I2PSocketManager mgr=_manager;
  _manager=null;
  _shitlist.clear();
  if (mgr != null)   mgr.destroySocketManager();
  FileUtil.rmdir(_tmpDir,false);
  _tmpDir.mkdirs();
}
