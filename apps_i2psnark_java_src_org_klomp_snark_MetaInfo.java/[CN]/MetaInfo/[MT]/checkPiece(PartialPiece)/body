{
  MessageDigest sha1=SHA1.getInstance();
  int piece=pp.getPiece();
  byte[] hash=pp.getHash();
  for (int i=0; i < 20; i++)   if (hash[i] != piece_hashes[20 * piece + i])   return false;
  return true;
}
