{
  if (torrentdata == null) {
    Map m=new HashMap();
    m.put("announce",announce);
    Map info=createInfoMap();
    m.put("info",info);
    torrentdata=BEncoder.bencode(m);
  }
  return torrentdata;
}
