{
  if (bs != null) {
    din.readFully(bs,off,len);
  }
 else {
    byte[] tmp=new byte[len];
    din.readFully(tmp);
synchronized (this) {
      if (raf == null)       createTemp();
      raf.seek(off);
      raf.write(tmp);
    }
  }
}
