{
  PeerState s=state;
  if (s != null) {
    PeerConnectionOut out=s.out;
    if (out != null)     return System.currentTimeMillis() - out.lastSent;
 else     return -1;
  }
 else {
    return -1;
  }
}
