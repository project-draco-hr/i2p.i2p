{
  if (_log.shouldLog(Log.INFO))   _log.info("Disconnected " + peer);
synchronized (peers) {
    if (peers.remove(peer)) {
      unchokePeer();
      removePeerFromPieces(peer);
    }
    peerCount=peers.size();
  }
  if (listener != null)   listener.peerChange(this,peer);
}
