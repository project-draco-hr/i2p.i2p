{
  int size=metainfo.getPieceLength(0);
  int max=_util.getMaxConnections();
  if (size <= 1024 * 1024)   return max;
  if (size <= 2 * 1024 * 1024)   return (max + 1) / 2;
  return (max + 3) / 4;
}
