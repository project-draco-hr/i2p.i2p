{
  if (listener != null)   listener.peerChange(this,peer);
synchronized (wantedPieces) {
    for (    Piece pc : wantedPieces) {
      if (pc.getId() == piece) {
        pc.addPeer(peer);
        return true;
      }
    }
    return false;
  }
}
