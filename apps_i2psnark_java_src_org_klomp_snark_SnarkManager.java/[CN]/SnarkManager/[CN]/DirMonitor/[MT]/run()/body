{
  try {
    Thread.sleep(60 * 1000 * getStartupDelayMinutes());
  }
 catch (  InterruptedException ie) {
  }
  while (true) {
    File dir=getDataDir();
    _log.debug("Directory Monitor loop over " + dir.getAbsolutePath());
    monitorTorrents(dir);
    try {
      Thread.sleep(60 * 1000);
    }
 catch (    InterruptedException ie) {
    }
  }
}
