{
  if (shouldRemove) {
synchronized (_snarks) {
      _snarks.remove(torrent.getName());
    }
  }
  boolean wasStopped=torrent.isStopped();
  torrent.stopTorrent();
  if (!wasStopped)   addMessage(_t("Torrent stopped: \"{0}\"",torrent.getBaseName()));
  if (shouldRemove)   removeTorrentStatus(torrent);
}
