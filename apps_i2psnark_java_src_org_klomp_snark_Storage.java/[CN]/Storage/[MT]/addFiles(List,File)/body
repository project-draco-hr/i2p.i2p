{
  if (!f.isDirectory())   l.add(f);
 else {
    File[] files=f.listFiles();
    if (files == null) {
      Snark.debug("WARNING: Skipping '" + f + "' not a normal file.",Snark.WARNING);
      return;
    }
    for (int i=0; i < files.length; i++)     addFiles(l,files[i]);
  }
}
