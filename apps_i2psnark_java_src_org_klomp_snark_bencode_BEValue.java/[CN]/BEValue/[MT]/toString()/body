{
  String valueString;
  if (value instanceof byte[]) {
    byte[] bs=(byte[])value;
    if (bs.length <= 12)     valueString=new String(bs);
 else     valueString="bytes:" + bs.length;
  }
 else   valueString=value.toString();
  return "BEValue[" + valueString + "]";
}
