{
  Peers peers=_torrents.get(ih);
  if (peers == null)   return Collections.emptyList();
  int size=peers.size();
  List<Hash> rv=new ArrayList<Hash>(peers.values());
  if (max < size) {
    Collections.shuffle(rv,_context.random());
    rv=rv.subList(0,max);
  }
  return rv;
}
