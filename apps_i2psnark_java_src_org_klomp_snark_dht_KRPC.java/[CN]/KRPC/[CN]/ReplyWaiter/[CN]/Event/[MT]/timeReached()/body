{
  _sentQueries.remove(mid);
  if (onTimeout != null)   onTimeout.run();
  timeout(ReplyWaiter.this);
  if (_log.shouldLog(Log.INFO))   _log.warn("timeout waiting for reply from " + ReplyWaiter.this.toString());
}
