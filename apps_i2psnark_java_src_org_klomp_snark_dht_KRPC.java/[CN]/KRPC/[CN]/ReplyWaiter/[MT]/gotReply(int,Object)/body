{
  cancel();
  _sentQueries.remove(mid);
  replyCode=code;
  replyObject=o;
  if (!sentTo.getNID().equals(_fakeNID))   heardFrom(sentTo);
  if (onReply != null)   onReply.run();
synchronized (this) {
    this.notifyAll();
  }
}
