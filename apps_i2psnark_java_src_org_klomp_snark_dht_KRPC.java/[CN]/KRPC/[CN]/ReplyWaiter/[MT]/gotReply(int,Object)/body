{
  replyCode=code;
  replyObject=o;
  event.cancel();
  _sentQueries.remove(mid);
  if (!getNID().equals(_fakeNID))   heardFrom(this);
  if (onReply != null)   onReply.run();
synchronized (this) {
    this.notifyAll();
  }
}
