{
  replyCode=code;
  replyObject=o;
  if (event != null)   event.cancel();
  _sentQueries.remove(mid);
  heardFrom(this);
  if (onReply != null)   onReply.run();
synchronized (this) {
    this.notifyAll();
  }
}
