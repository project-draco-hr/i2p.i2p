{
  if (_log.shouldLog(Log.INFO))   _log.info("Sending ping to: " + nInfo);
  Map<String,Object> map=new HashMap();
  map.put("q","ping");
  Map<String,Object> args=new HashMap();
  map.put("a",args);
  return sendQuery(nInfo,map,true);
}
