{
  if (_log.shouldLog(Log.INFO))   _log.info("Sending pong to: " + nInfo);
  Map<String,Object> map=new HashMap();
  Map<String,Object> resps=new HashMap();
  map.put("r",resps);
  return sendResponse(nInfo,msgID,map);
}
