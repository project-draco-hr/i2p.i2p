{
  if (bytes < 5 * 1024)   return bytes + "B";
 else   if (bytes < 5 * 1024 * 1024)   return (bytes / 1024) + "KB";
 else   if (bytes < 5 * 1024 * 1024* 1024)   return (bytes / (1024 * 1024)) + "MB";
 else   return (bytes / (1024 * 1024 * 1024)) + "GB";
}
