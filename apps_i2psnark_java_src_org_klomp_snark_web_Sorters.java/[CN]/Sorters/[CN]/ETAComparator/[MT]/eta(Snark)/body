{
  long needed=snark.getNeededLength();
  long total=snark.getTotalLength();
  if (needed > total)   needed=total;
  long remainingSeconds;
  long downBps=snark.getDownloadRate();
  if (downBps > 0 && needed > 0)   return needed / downBps;
  return -1;
}
