{
  if (_log.shouldLog(Log.INFO))   _log.info("Closing task " + t.getId() + (forced ? " forced..." : "..."));
  if (t.close(forced)) {
    if (_log.shouldLog(Log.INFO))     _log.info("Task " + t.getId() + " closed.");
    return true;
  }
  return false;
}
