{
  try {
    out.flush();
  }
 catch (  IOException ioe) {
  }
  try {
    i2pout.flush();
  }
 catch (  IOException ioe) {
  }
  in.close();
  i2pin.close();
  s.close();
  i2ps.close();
  t1.join(30 * 1000);
  t2.join(30 * 1000);
}
