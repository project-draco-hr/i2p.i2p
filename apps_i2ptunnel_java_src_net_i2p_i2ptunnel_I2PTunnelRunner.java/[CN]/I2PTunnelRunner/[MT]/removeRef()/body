{
  if (sockList != null) {
synchronized (slock) {
      boolean removed=sockList.remove(i2ps);
      System.out.println("Removal of i2psocket " + i2ps + " successful? "+ removed+ " remaining: "+ sockList.size());
    }
  }
}
