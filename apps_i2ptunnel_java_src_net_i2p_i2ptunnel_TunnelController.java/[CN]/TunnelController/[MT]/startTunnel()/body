{
  _starting=true;
  try {
    doStartTunnel();
  }
 catch (  Exception e) {
    _log.error("Error starting up the tunnel",e);
    log("Error starting up the tunnel - " + e.getMessage());
    acquire();
    stopTunnel();
  }
  _starting=false;
}
