{
  _incoming.remove(lport);
  _active.remove(lport);
  if (_log.shouldLog(Log.WARN))   _log.warn("Removed client tunnel for port " + lport + " pending count now: "+ _incoming.size()+ " active count now: "+ _active.size());
}
