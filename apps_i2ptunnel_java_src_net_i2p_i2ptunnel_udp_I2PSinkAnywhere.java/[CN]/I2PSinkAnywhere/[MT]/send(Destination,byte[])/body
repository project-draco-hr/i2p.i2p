{
  byte[] payload;
  if (!this.raw)   payload=this.maker.makeI2PDatagram(data);
 else   payload=data;
  try {
    this.sess.sendMessage(to,payload);
  }
 catch (  I2PSessionException exc) {
    exc.printStackTrace();
  }
}
