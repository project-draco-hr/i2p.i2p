{
  String stype=getProperty(tunnel,I2PClient.PROP_SIGTYPE,"0");
  if (stype.equals("0"))   return 0;
  SigType type=SigType.parseSigType(stype);
  if (type == null)   return 0;
  return type.getCode();
}
