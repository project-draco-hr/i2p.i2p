{
  TunnelController tun=getController(tunnel);
  if (tun != null) {
    if (tunnel != 0 && "true".equalsIgnoreCase(tun.getSharedClient()) && isClient(tunnel))     return "Listed above";
 else     return tun.getHTMLStreams();
  }
 else   return "";
}
