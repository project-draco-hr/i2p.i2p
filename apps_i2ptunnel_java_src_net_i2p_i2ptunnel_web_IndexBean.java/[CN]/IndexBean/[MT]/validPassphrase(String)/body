{
  if (proposed == null)   return false;
  String pass=_context.getProperty(PROP_TUNNEL_PASSPHRASE);
  if ((pass != null) && (pass.trim().length() > 0))   return pass.trim().equals(proposed.trim());
 else   return false;
}
