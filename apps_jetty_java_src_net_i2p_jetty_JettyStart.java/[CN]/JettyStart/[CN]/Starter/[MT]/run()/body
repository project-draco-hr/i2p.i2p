{
  changeState(STARTING);
  for (  LifeCycle lc : _jettys) {
    if (!lc.isRunning()) {
      try {
        lc.start();
      }
 catch (      Exception e) {
        changeState(START_FAILED,e);
        return;
      }
    }
  }
  changeState(RUNNING);
}
