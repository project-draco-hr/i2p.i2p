{
synchronized (flagLock) {
    if (_log.shouldLog(Log.DEBUG))     _log.debug("Closing connection");
    closed=true;
    _closedOn=I2PAppContext.getGlobalContext().clock().now();
  }
  out.close();
  in.notifyClosed();
}
