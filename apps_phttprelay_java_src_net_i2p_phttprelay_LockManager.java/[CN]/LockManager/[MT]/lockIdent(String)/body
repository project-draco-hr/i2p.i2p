{
  while (true) {
synchronized (_locks) {
      if (!_locks.contains(target)) {
        _locks.add(target);
        return;
      }
      try {
        _locks.wait(1000);
      }
 catch (      InterruptedException ie) {
      }
    }
  }
}
