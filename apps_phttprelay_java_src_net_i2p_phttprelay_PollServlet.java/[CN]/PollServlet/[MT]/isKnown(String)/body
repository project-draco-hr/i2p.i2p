{
  File identDir=getIdentDir(target);
  if (identDir.exists()) {
    File identFile=new File(identDir,"identity.dat");
    if (identFile.exists()) {
      return true;
    }
 else {
      return false;
    }
  }
 else {
    return false;
  }
}
