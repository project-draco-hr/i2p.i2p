{
  while (true) {
    try {
      if (items.size() == 0)       wait();
      Object item=items.get(0);
      items.remove(0);
      return item;
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
  }
}
