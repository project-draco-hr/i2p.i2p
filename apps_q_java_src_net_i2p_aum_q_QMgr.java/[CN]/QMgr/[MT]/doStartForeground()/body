{
  QNode node;
  try {
    if (isServer) {
      node=new QServerNode(nodeDirStr);
    }
 else {
      node=new QClientNode(nodeDirStr);
    }
    node.run();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  return 0;
}
