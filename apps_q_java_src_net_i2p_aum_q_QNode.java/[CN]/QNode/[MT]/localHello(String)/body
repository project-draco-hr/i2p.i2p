{
  Hashtable h=new Hashtable();
  try {
    newPeer(destBase64);
    h.put("status","ok");
  }
 catch (  Exception e) {
    e.printStackTrace();
    h.put("status","error");
    h.put("error",e.getClass().getName());
  }
  return h;
}
