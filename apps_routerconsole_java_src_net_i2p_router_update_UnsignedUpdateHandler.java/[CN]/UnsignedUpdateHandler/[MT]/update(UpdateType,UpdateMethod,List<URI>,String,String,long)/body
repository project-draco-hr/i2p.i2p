{
  if (type != UpdateType.ROUTER_UNSIGNED || method != UpdateMethod.HTTP || updateSources.isEmpty())   return null;
  UpdateRunner update=new UnsignedUpdateRunner(_context,updateSources);
  update.start();
  return update;
}
