{
  RouterAddress addr=_context.router().getRouterInfo().getTargetAddress("SSU");
  if (addr == null)   return _t("unknown");
  String rv=addr.getHost();
  if (rv == null)   return _t("unknown");
  return rv;
}
