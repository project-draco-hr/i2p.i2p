{
  RouterAddress addr=_context.router().getRouterInfo().getTargetAddress("SSU");
  if (addr == null)   return _("unknown");
  UDPAddress ua=new UDPAddress(addr);
  if (ua.getPort() <= 0)   return _("unknown");
  return "" + ua.getPort();
}
