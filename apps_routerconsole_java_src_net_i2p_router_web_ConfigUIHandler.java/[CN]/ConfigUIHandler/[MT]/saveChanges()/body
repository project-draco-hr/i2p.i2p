{
  if (_config == null)   return;
  if (_config.equals("default"))   _context.router().removeConfigSetting(ConfigUIHelper.PROP_THEME);
 else   _context.router().setConfigSetting(ConfigUIHelper.PROP_THEME,_config);
  if (_context.router().saveConfig())   addFormNotice("Configuration saved successfully");
 else   addFormNotice("Error saving the configuration (applied but not saved) - please see the error logs");
}
