{
  String url=ctx.getProperty(ConfigUpdateHandler.PROP_NEWS_URL);
  if (url != null && !url.equals(ConfigUpdateHandler.OLD_DEFAULT_NEWS_URL))   return url;
 else   return ConfigUpdateHandler.DEFAULT_NEWS_URL;
}
