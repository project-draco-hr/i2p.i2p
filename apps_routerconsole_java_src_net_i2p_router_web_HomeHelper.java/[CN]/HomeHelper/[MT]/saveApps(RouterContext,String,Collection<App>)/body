{
  StringBuilder buf=new StringBuilder(1024);
  for (  App app : apps) {
    buf.append(app.name).append(S).append(app.desc).append(S).append(app.url).append(S).append(app.icon).append(S);
  }
  ctx.router().saveConfig(prop,buf.toString());
}
