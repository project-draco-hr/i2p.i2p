{
  if (_context.getBooleanPropertyDefaultTrue("plugins.autoUpdate") && (!NewsHelper.isUpdateInProgress()) && (!RouterVersion.VERSION.equals(_context.getProperty("router.previousVersion"))))   updateAll(_context,true);
  startPlugins(_context);
}
