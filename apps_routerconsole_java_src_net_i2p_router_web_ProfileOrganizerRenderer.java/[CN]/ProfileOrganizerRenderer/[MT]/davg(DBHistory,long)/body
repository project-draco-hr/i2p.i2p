{
  RateStat rs=dbh.getFailedLookupRate();
  if (rs == null)   return _(NA);
  Rate r=rs.getRate(rate);
  if (r == null)   return _(NA);
  long c=r.getCurrentEventCount() + r.getLastEventCount();
  return "" + c;
}
