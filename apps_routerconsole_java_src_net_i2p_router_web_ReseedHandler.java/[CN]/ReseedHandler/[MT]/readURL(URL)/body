{
  ByteArrayOutputStream baos=new ByteArrayOutputStream(4 * 1024);
  EepGet get=new EepGet(I2PAppContext.getGlobalContext(),false,null,-1,0,null,baos,url.toString(),false,null,null);
  if (get.fetch())   return baos.toByteArray();
 else   return null;
}
