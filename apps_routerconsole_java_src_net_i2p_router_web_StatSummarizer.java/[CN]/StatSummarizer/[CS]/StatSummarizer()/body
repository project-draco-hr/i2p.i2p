{
  _context=(RouterContext)RouterContext.listContexts().get(0);
  _log=_context.logManager().getLog(getClass());
  _listeners=new ArrayList(16);
  _instance=this;
  _sem=new Semaphore(MAX_CONCURRENT_PNG,true);
}
