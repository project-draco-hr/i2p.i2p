{
  if (_context == null)   return "0 ms";
  RateStat rs=_context.statManager().getRate("jobQueue.jobLag");
  if (rs == null)   return "0 ms";
  Rate lagRate=rs.getRate(60 * 1000);
  return DataHelper.formatDuration2((long)lagRate.getAverageValue());
}
