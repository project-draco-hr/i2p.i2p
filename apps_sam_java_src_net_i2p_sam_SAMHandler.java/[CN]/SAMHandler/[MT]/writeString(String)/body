{
  if (_log.shouldLog(Log.DEBUG))   _log.debug("Sending the client: [" + str + "]");
  try {
    writeBytes(str.getBytes("ISO-8859-1"));
  }
 catch (  IOException e) {
    _log.debug("Caught IOException",e);
    return false;
  }
  return true;
}
