{
  if (data.length > RAW_SIZE_MAX)   throw new DataFormatException("Data size limit exceeded (" + data.length + ")");
  return sendBytesThroughMessageSession(dest,data,I2PSession.PROTO_DATAGRAM_RAW,I2PSession.PORT_UNSPECIFIED,I2PSession.PORT_UNSPECIFIED);
}
