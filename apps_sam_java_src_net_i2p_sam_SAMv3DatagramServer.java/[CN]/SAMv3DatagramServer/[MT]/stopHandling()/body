{
synchronized (SAMv3DatagramServer.class) {
    if (server != null) {
      try {
        server.close();
      }
 catch (      IOException ioe) {
      }
      server=null;
    }
  }
  _listener.interrupt();
  if (_parent != null)   _parent.unregister(this);
}
