def byteoutstream2str(bs):
    '\n    Convenience - converts java-friendly byteoutputstream to python string\n    '
    chars = []
    while 1:
        c = bs.read()
        if (c >= 0):
            chars.append(chr(c))
        else:
            break
    return ''.join(chars)
