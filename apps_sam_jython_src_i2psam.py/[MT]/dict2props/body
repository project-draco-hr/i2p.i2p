def dict2props(d):
    '\n    Converts a python dict d into a java.util.Properties object\n    '
    props = java.util.Properties()
    for (k, v) in d.items():
        props[k] = str(v)
    return props
