def str2bytearray(s):
    '\n    Convenience - converts python string to java-friendly byte array\n    '
    a = []
    for c in s:
        n = ord(c)
        if (n >= 128):
            n = (n - 256)
        a.append(n)
    return a
