from i2p import sam
import time
S = sam.socket('Eve', sam.SOCK_RAW)
print 'Serving at:', S.dest
S.setblocking(False)
while True:
    try:
        data = S.recv(1000)
        print 'Got', data
    except sam.BlockError:
        pass
    time.sleep(0.01)
