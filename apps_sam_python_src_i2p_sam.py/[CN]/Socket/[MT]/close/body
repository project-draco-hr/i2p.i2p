def close(self):
    try:
        self._verify_connected()
        connected = True
    except i2p.Error:
        connected = False
    if connected:
        self.sessobj.close()
    else:
        pass
    self.closed = True
