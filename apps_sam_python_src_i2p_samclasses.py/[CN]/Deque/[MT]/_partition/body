def _partition(self):
    if (len(self) > 1):
        self.a.reverse()
        all = (self.a + self.b)
        n = (len(all) / 2)
        self.a = all[:n]
        self.b = all[n:]
        self.a.reverse()
