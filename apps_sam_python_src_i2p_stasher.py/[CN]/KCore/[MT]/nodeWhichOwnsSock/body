def nodeWhichOwnsSock(self, sock):
    '\n        returns ref to node which owns a socket\n        '
    for node in self.nodes:
        if (node.sock == sock):
            return node
    return None
