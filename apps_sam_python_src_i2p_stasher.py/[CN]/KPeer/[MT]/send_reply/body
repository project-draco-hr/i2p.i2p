def send_reply(self, **kw):
    '\n        Sends an RPC reply back to upstream peer\n        '
    self.log(5, ('\nnode %s\nreplying to peer %s:\n%s' % (self.node, self, kw)))
    self.send_raw(type='reply', **kw)
