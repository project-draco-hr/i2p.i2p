def changeState(self, oldstate, newstate):
    "\n        for all recs of state 'oldstate', change their\n        state to 'newstate'\n        "
    for p in self.peers:
        if (p.state == oldstate):
            p.state = newstate
