def close(self):
    '\n        close this stream connection\n        '
    log(4, 'closing stream')
    self.client.samStreamClose(self)
    log(4, 'stream closed')
    self.isOpen = 0
    self.qIncomingData.put('')
