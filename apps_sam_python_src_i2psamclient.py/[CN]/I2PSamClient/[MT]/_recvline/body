def _recvline(self):
    '\n        Guaranteed read of a full line\n        '
    chars = []
    while 1:
        c = self.sock.recv(1)
        if (c in ['', '\n']):
            break
        chars.append(c)
    return ''.join(chars)
