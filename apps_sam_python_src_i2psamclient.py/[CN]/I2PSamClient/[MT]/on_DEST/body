def on_DEST(self, topic, subtopic, args):
    '\n        Handles DEST messages from server\n        '
    pubkey = args['PUB']
    privkey = args['PRIV']
    res = (pubkey, privkey)
    self.qNewDests.put(res)
