def samDatagramSend(self, peerdest, msg):
    '\n        Sends a repliable datagram message to another peer\n    \n        peerdest is the public base64 destination key of the peer\n        '
    self.samSend('DATAGRAM', 'SEND', msg, DESTINATION=peerdest)
