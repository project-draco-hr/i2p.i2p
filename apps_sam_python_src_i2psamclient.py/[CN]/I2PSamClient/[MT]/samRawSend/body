def samRawSend(self, peerdest, msg):
    '\n        Sends a raw anon message to another peer\n        \n        peerdest is the public base64 destination key of the peer\n        '
    self.samSend('RAW', 'SEND', msg, DESTINATION=peerdest)
