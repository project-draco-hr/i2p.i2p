def bdecode_rec(x, f):
    t = x[f]
    if (t == 'i'):
        return decode_int(x, (f + 1))
    elif (t == 'l'):
        return decode_list(x, (f + 1))
    elif (t == 'd'):
        return decode_dict(x, (f + 1))
    else:
        return decode_string(x, f)
