def getref(self):
    "\n        Uplifts node's own ref\n        "
    self.connect()
    self.write('getref\n')
    self.flush()
    res = self.readline().strip()
    self.close()
    if (res == 'ok'):
        ref = self.readline().strip()
        return ref
    else:
        return None
