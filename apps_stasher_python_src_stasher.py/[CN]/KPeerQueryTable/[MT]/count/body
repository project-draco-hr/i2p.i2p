def count(self, *args):
    '\n        returns the number of records whose state is one of args\n        '
    count = 0
    for rec in self.peers:
        if (rec.state in args):
            count += 1
    return count
