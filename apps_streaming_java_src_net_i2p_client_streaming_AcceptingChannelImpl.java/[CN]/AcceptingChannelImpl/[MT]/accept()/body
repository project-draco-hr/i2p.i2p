{
  I2PSocket sock;
  try {
    sock=socket.accept();
  }
 catch (  SocketTimeoutException ex) {
    return null;
  }
synchronized (this) {
    I2PSocket temp=next;
    next=sock;
    return temp;
  }
}
