{
  _max=max;
  _totalMax=totalMax;
  if (max > 0) {
    SimpleScheduler.getInstance().addPeriodicEvent(new Cleaner(),period);
    this.counter=new ObjectCounter();
  }
 else {
    this.counter=null;
  }
  if (totalMax > 0)   _currentTotal=new AtomicInteger();
 else   _currentTotal=null;
}
