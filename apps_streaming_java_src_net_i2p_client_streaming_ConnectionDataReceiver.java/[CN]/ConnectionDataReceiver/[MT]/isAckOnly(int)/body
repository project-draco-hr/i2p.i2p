{
  boolean ackOnly=((size <= 0) && (_connection.getLastSendId() >= 0) && ((!_connection.getOutputStream().getClosed()) || (_connection.getOutputStream().getClosed() && _connection.getCloseSentOn() > 0)));
  return ackOnly;
}
