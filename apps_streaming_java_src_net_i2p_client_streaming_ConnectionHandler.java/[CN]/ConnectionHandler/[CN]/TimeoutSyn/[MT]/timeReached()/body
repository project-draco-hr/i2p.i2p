{
  boolean removed=_synQueue.remove(_synPacket);
  if (removed) {
    if (_synPacket.isFlagSet(Packet.FLAG_SYNCHRONIZE))     sendReset(_synPacket);
 else     reReceivePacket(_synPacket);
  }
 else {
  }
}
