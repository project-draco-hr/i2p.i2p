{
  Destination from=syn.getOptionalFrom();
  if (from == null)   return true;
  if (_defaultOptions.isAccessListEnabled())   return !_defaultOptions.getAccessList().contains(from.calculateHash());
  if (_defaultOptions.isBlacklistEnabled())   return _defaultOptions.getBlacklist().contains(from.calculateHash());
  return false;
}
