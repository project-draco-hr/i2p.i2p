{
  if (_con.getLastSendTime() <= _created) {
    if (_con.getResetReceived() || _con.getResetSent() || (_con.getUnackedPacketsReceived() <= 0))     return;
    if (_log.shouldLog(Log.DEBUG))     _log.debug("Last sent was a while ago, and we want to ack a dup");
    _con.ackImmediately();
  }
}
