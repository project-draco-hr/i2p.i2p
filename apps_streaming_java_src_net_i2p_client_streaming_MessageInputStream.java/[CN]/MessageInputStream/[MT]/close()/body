{
synchronized (_dataLock) {
    _readyDataBlocks.clear();
    for (    ByteArray ba : _notYetReadyBlocks.values()) {
      ba.setData(null);
    }
    _locallyClosed=true;
    _dataLock.notifyAll();
  }
}
