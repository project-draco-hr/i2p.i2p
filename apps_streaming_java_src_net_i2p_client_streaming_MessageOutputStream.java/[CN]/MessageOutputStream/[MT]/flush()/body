{
synchronized (_dataLock) {
    if (_log.shouldLog(Log.DEBUG))     _log.debug("flush(): valid = " + _valid);
    _dataReceiver.writeData(_buf,0,_valid);
    if (_log.shouldLog(Log.DEBUG))     _log.debug("flush(): valid = " + _valid + " complete");
    _valid=0;
  }
  throwAnyError();
}
