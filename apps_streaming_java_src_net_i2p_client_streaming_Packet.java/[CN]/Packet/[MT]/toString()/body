{
  return "Packet " + _sequenceNum + " on "+ toId(_sendStreamId)+ "<-->"+ toId(_receiveStreamId)+ ": "+ toFlagString()+ " ACK through "+ _ackThrough+ " size: "+ (_payload != null ? _payload.length : 0);
}
