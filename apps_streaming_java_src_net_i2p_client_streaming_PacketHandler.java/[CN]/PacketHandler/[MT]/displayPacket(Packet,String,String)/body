{
  String msg=null;
synchronized (_fmt) {
    msg=_fmt.format(new Date()) + ": " + prefix+ " "+ packet.toString()+ (suffix != null ? " " + suffix : "");
  }
  if (_log.shouldLog(Log.DEBUG))   System.out.println(msg);
}
