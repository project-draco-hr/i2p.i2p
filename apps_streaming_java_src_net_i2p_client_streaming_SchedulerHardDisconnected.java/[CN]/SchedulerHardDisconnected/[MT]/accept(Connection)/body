{
  if (con == null)   return false;
  long timeSinceClose=_context.clock().now() - con.getCloseSentOn();
  boolean ok=(con.getHardDisconnected()) && (timeSinceClose < Connection.DISCONNECT_TIMEOUT);
  return ok;
}
