{
  for (Iterator iter=_cache.keySet().iterator(); iter.hasNext(); ) {
    if (_cache.get(iter.next()).isExpired())     iter.remove();
  }
  SimpleTimer.getInstance().addEvent(CleanEvent.this,CLEAN_TIME);
}
