{
  if (_log.shouldLog(Log.DEBUG))   _log.debug("setActive(" + active + ") called");
  _active=active;
  if (!active) {
    try {
      _synQueue.put(new PoisonPacket());
    }
 catch (    InterruptedException ie) {
    }
  }
}
