{
  O result=null;
  if (elements != null) {
    int pageSize=getPageSize();
    int offset=(currentPage - 1) * pageSize;
    offset+=x;
    if (offset >= 0 && offset < elements.length)     result=elements[offset];
  }
  return result;
}
