{
  if (tags == null)   return new ArrayList();
  StringTokenizer tok=new StringTokenizer(tags," ,\t\n");
  List rv=new ArrayList();
  while (tok.hasMoreTokens()) {
    String cur=tok.nextToken().trim();
    if (cur.length() > 0)     rv.add(cur);
  }
  return rv;
}
