{
  int i;
  for (i=0; i < s.length(); i++) {
    if (s.charAt(i) == '>')     return i + 1;
    if (s.charAt(i) == '"') {
      i++;
      while (s.charAt(i) != '"')       i++;
    }
  }
  System.out.println("WTF");
  return 0;
}
