{
  BlogURI l=(BlogURI)lhs;
  BlogURI r=(BlogURI)rhs;
  if (l.getEntryId() > r.getEntryId())   return -1;
 else   if (l.getEntryId() < r.getEntryId())   return 1;
 else   return DataHelper.compareTo(l.getKeyHash().getData(),r.getKeyHash().getData());
}
