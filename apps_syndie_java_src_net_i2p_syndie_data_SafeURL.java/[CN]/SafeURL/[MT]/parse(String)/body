{
  if (raw != null) {
    int index=raw.indexOf("://");
    if ((index <= 0) || (index + 1 >= raw.length()))     return;
    _schema=raw.substring(0,index);
    _location=raw.substring(index + 3);
    _location.replace('>','_');
    _location.replace('<','^');
  }
}
