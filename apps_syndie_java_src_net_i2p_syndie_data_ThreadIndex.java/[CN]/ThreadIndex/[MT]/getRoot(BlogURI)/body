{
  ThreadNode node=getNode(uri);
  if (node == null)   return -1;
  while (node.getParent() != null)   node=node.getParent();
  for (int i=0; i < getRootCount(); i++) {
    ThreadNode cur=getRoot(i);
    if (cur.equals(node))     return i;
  }
  return -1;
}
