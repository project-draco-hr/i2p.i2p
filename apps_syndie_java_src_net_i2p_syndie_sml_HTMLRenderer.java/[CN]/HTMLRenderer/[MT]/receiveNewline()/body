{
  if (!continueBody()) {
    return;
  }
  if (_log.shouldLog(Log.DEBUG))   _log.debug("receive NL");
  if (true || (_lastNewlineAt >= _bodyBuffer.length()))   _bodyBuffer.append(getSpan("nl")).append("<br /></span>\n");
 else   _lastNewlineAt=_bodyBuffer.length();
}
