{
  if ((style == null) || (style.trim().length() <= 0))   return null;
  char c[]=style.toCharArray();
  for (int i=0; i < c.length; i++)   if (STYLE_CHARS.indexOf(c[i]) < 0)   c[i]='_';
  return new String(c);
}
