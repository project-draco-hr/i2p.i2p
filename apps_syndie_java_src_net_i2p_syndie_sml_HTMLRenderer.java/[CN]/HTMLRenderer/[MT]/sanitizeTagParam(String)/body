{
  if (str == null)   return "";
  str=str.replaceAll("&","&amp;");
  if (str.indexOf("\"") < 0 && str.indexOf("'") < 0)   return sanitizeString(str);
  str=str.replaceAll("\"","&quot;");
  str=str.replaceAll("'","&#39;");
  return sanitizeString(str);
}
