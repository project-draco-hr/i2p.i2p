{
  if (empty(reqProto))   return false;
  if (proto == null) {
    if (PROTO_ARCHIVE.equals(reqProto) || PROTO_BLOG.equals(reqProto) || PROTO_EEPSITE.equals(reqProto)|| PROTO_I2PHEX.equals(reqProto))     return false;
 else     return true;
  }
 else {
    return proto.equals(reqProto);
  }
}
