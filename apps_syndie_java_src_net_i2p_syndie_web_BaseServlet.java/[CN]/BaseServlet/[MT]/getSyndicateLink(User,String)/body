{
  if ((user != null) && (archiveName != null)) {
    PetName pn=user.getPetNameDB().getByName(archiveName);
    if (pn != null)     return "syndicate.jsp?" + ThreadedHTMLRenderer.PARAM_ARCHIVE + "="+ pn.getLocation();
  }
  return "syndicate.jsp";
}
