{
  String groupName=null;
  if (pn.getGroupCount() > 0)   groupName=pn.getGroup(0);
  List group=getGroup(groupName);
  if (group != null) {
    group.remove(pn);
    if (group.size() <= 0)     _groups.remove(group);
  }
  _updated=true;
}
