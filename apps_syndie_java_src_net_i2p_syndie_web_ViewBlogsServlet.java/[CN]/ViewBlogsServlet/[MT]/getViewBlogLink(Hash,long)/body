{
  long dayBegin=BlogManager.instance().getDayBegin();
  int daysAgo=2;
  if ((lastPost > 0) && (dayBegin - 3 * 24 * 60* 60* 1000l >= lastPost))   daysAgo=(int)((dayBegin - lastPost + 24 * 60 * 60* 1000l - 1) / (24 * 60 * 60* 1000l));
  daysAgo++;
  return "blog.jsp?" + ViewBlogServlet.PARAM_BLOG + "="+ blog.toBase64();
}
