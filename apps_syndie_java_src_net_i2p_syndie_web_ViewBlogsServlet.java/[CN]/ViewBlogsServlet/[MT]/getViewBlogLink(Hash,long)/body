{
  long dayBegin=BlogManager.instance().getDayBegin();
  int daysAgo=2;
  if ((lastPost > 0) && (dayBegin - 3 * 24 * 60* 6081000 > lastPost))   daysAgo=(int)((dayBegin - lastPost + 24 * 60 * 60* 1000 - 1) / (24 * 60 * 60* 1000));
  daysAgo++;
  return getControlTarget() + "?" + ThreadedHTMLRenderer.PARAM_AUTHOR+ '='+ blog.toBase64()+ '&'+ ThreadedHTMLRenderer.PARAM_THREAD_AUTHOR+ "=true&daysBack="+ daysAgo;
}
