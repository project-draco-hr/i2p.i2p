{
  _commandThread=new CommandThread(Thread.currentThread(),shellCommand,CONSUME_OUTPUT);
  _commandThread.start();
  try {
    if (seconds > 0) {
      wait(seconds * 1000);
      return true;
    }
  }
 catch (  InterruptedException e) {
  }
  if (_commandSuccessful)   return true;
  return false;
}
