{
  String browser=null;
  UrlLauncher urlLauncher=new UrlLauncher();
  if (_browserString == null || _browserString.equals("default")) {
    try {
      if (urlLauncher.openUrl(url))       return;
    }
 catch (    RuntimeException ex) {
    }
  }
 else {
    try {
      if (urlLauncher.openUrl(url,_browserString))       return;
    }
 catch (    RuntimeException ex) {
    }
  }
  if (!(browser=promptForBrowser("Please select another browser")).equals("nullnull"))   setBrowser(browser);
}
