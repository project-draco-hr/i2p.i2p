{
  UrlLauncher launcher=new UrlLauncher();
  try {
    if (args.length > 0)     launcher.openUrl(args[0]);
 else     launcher.openUrl("http://127.0.0.1:7657/index.jsp");
  }
 catch (  Exception e) {
  }
}
