{
  CPUIDResult cachedCPUID=cpuidCache.get(iFunction);
  if (cachedCPUID != null)   return cachedCPUID;
 else {
    CPUIDResult c=doCPUID(iFunction);
    cpuidCache.put(iFunction,c);
    return c;
  }
}
