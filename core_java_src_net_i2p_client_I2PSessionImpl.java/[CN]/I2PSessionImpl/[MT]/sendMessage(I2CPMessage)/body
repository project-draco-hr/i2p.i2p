{
  if (isClosed())   throw new I2PSessionException("Already closed");
 else   if (_queue != null)   _queue.offer(message);
 else   if (_writer == null)   throw new I2PSessionException("Already closed");
 else   _writer.addMessage(message);
}
