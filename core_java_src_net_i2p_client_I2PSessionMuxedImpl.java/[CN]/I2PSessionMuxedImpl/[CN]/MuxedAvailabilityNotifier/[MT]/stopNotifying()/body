{
  boolean again=true;
  _msgs.clear();
  if (_alive.get()) {
    while (again) {
      _msgs.clear();
      try {
        _msgs.put(new MsgData(0,POISON_SIZE,0,0,0));
        again=false;
      }
 catch (      InterruptedException ie) {
        continue;
      }
    }
  }
  _alive.set(false);
}
