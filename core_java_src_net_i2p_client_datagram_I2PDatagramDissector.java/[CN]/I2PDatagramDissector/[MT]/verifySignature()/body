{
  if (this.valid)   return;
  if (!dsaEng.verifySignature(rxSign,rxHashBytes,rxDest.getSigningPublicKey()))   throw new I2PInvalidDatagramException("Incorrect I2P repliable datagram signature");
  this.valid=true;
}
