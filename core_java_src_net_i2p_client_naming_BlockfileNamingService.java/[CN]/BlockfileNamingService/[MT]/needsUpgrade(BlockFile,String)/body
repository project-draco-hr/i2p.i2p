{
  if (version != null && VersionComparator.comp(version,VERSION) >= 0)   return false;
  if (!bf.file.canWrite()) {
    if (_log.shouldLog(Log.WARN))     _log.warn("Not upgrading read-only database version " + version);
    return false;
  }
  return true;
}
