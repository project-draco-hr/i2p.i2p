{
  if (key == null)   return false;
  boolean rv=key.length() > 0 && de != null && de.dest != null && de.dest.getPublicKey() != null;
  if (!rv)   _invalid.add(new InvalidEntry(key,listname));
  return rv;
}
