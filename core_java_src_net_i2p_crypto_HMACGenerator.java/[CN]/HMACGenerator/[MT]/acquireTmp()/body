{
  byte rv[]=null;
synchronized (_availableTmp) {
    if (!_availableTmp.isEmpty())     rv=(byte[])_availableTmp.remove(0);
  }
  if (rv != null)   Arrays.fill(rv,(byte)0x0);
 else   rv=new byte[Hash.HASH_LENGTH];
  return rv;
}
