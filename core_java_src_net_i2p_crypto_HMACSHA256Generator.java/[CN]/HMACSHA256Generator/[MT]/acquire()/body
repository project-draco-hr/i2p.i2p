{
synchronized (_available) {
    if (_available.size() > 0)     return (HMac)_available.remove(0);
  }
  if (_useMD5)   return new HMac(new MD5Digest(),32);
 else   return new HMac(new SHA256Digest(),32);
}
