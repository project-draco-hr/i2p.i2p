{
  byte buf[]=new byte[length];
  if (src.length > buf.length)   System.arraycopy(src,src.length - buf.length,buf,0,buf.length);
 else   if (src.length < buf.length)   System.arraycopy(src,0,buf,buf.length - src.length,src.length);
 else   System.arraycopy(src,0,buf,0,buf.length);
  return buf;
}
