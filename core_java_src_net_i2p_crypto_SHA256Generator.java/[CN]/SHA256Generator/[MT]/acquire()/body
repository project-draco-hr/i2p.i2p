{
  SHA256Digest rv=null;
synchronized (_digests) {
    if (_digests.size() > 0)     rv=(SHA256Digest)_digests.remove(0);
  }
  if (rv != null)   rv.reset();
 else   rv=new SHA256Digest();
  return rv;
}
