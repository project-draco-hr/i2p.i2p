{
  try {
    int idx=0;
    if (d[idx++] != 0x30 || d[idx++] != 37 || d[idx++] != 0x0a || d[idx++] != 1 || d[idx++] != 1 || d[idx++] != 0x04 || d[idx++] != 32)     throw new InvalidKeySpecException("unsupported key spec");
    byte[] rv=new byte[32];
    System.arraycopy(d,idx,rv,0,32);
    return rv;
  }
 catch (  IndexOutOfBoundsException ioobe) {
    throw new InvalidKeySpecException(ioobe);
  }
}
