{
  if ((object == null) || !(object instanceof Destination))   return false;
  Destination dst=(Destination)object;
  return DataHelper.eq(_certificate,dst.getCertificate()) && DataHelper.eq(_signingKey,dst.getSigningPublicKey()) && DataHelper.eq(_publicKey,dst.getPublicKey());
}
