{
  if (data != null && data.length != HASH_LENGTH)   throw new IllegalArgumentException("Hash must be 32 bytes");
  _data=data;
  _stringified=null;
  _base64ed=null;
  _cachedHashCode=calcHashCode();
}
