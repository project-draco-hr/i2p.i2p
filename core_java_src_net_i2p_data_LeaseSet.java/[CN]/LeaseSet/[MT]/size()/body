{
  return PublicKey.KEYSIZE_BYTES + SigningPublicKey.KEYSIZE_BYTES + _destination.getCertificate().size()+ PublicKey.KEYSIZE_BYTES+ SigningPublicKey.KEYSIZE_BYTES+ 1+ _leases.size() * (Hash.HASH_LENGTH + 4 + 8);
}
