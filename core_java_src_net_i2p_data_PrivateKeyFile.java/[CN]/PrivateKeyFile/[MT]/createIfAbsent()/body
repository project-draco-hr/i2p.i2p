{
  if (!this.file.exists()) {
    FileOutputStream out=null;
    try {
      out=new FileOutputStream(this.file);
      if (this.client != null)       this.client.createDestination(out);
 else       write();
    }
  finally {
      if (out != null) {
        try {
          out.close();
        }
 catch (        IOException ioe) {
        }
      }
    }
  }
  return getDestination();
}
