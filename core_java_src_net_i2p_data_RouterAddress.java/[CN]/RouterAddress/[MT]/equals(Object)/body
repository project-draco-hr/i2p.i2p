{
  if ((object == null) || !(object instanceof RouterAddress))   return false;
  RouterAddress addr=(RouterAddress)object;
  return _cost == addr._cost && DataHelper.eq(_transportStyle,addr._transportStyle);
}
