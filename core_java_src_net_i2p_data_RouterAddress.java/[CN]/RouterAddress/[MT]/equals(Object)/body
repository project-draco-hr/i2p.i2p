{
  if ((object == null) || !(object instanceof RouterAddress))   return false;
  RouterAddress addr=(RouterAddress)object;
  return DataHelper.eq(_cost,addr.getCost()) && DataHelper.eq(_expiration,addr.getExpiration()) && DataHelper.eq(_options,addr.getOptions())&& DataHelper.eq(_transportStyle,addr.getTransportStyle());
}
