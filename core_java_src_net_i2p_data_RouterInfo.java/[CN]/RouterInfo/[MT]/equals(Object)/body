{
  if ((object == null) || !(object instanceof RouterInfo))   return false;
  RouterInfo info=(RouterInfo)object;
  return DataHelper.eq(_identity,info.getIdentity()) && DataHelper.eq(_signature,info.getSignature()) && _published == info.getPublished() && DataHelper.eq(_addresses,info.getAddresses()) && DataHelper.eq(_options,info.getOptions()) && DataHelper.eq(getPeers(),info.getPeers());
}
