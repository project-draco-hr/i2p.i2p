{
  if ((object == null) || !(object instanceof RouterInfo))   return false;
  RouterInfo info=(RouterInfo)object;
  return DataHelper.eq(_addresses,info.getAddresses()) && DataHelper.eq(_identity,info.getIdentity()) && DataHelper.eq(_options,info.getOptions())&& DataHelper.eq(_peers,info.getPeers())&& DataHelper.eq(_signature,info.getSignature())&& DataHelper.eq(getPublished(),info.getPublished());
}
