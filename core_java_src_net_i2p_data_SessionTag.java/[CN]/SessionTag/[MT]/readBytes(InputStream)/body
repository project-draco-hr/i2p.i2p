{
  byte data[]=new byte[BYTE_LENGTH];
  int read=DataHelper.read(in,data);
  if (read != BYTE_LENGTH)   throw new DataFormatException("Not enough data (read " + read + " wanted "+ BYTE_LENGTH+ ")");
  setData(data);
}
