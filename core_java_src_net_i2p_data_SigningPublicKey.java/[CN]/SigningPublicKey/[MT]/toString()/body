{
  StringBuffer buf=new StringBuffer(64);
  buf.append("[SigningPublicKey: ");
  if (_data == null) {
    buf.append("null key");
  }
 else {
    buf.append("size: ").append(_data.length);
  }
  buf.append("]");
  return buf.toString();
}
