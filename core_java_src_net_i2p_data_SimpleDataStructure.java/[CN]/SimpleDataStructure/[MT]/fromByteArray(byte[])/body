{
  if (data == null)   throw new DataFormatException("Null data passed in");
  if (data.length != _length)   throw new DataFormatException("Bad data length: " + data.length + "; required: "+ _length);
  setData(data);
}
