{
  if ((object != null) && (object instanceof MessagePayloadMessage)) {
    MessagePayloadMessage msg=(MessagePayloadMessage)object;
    return _sessionId == msg.getSessionId() && _messageId == msg.getMessageId() && DataHelper.eq(_payload,msg.getPayload());
  }
  return false;
}
