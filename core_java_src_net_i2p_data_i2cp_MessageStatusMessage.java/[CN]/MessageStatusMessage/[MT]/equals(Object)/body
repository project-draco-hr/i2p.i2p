{
  if ((object != null) && (object instanceof MessageStatusMessage)) {
    MessageStatusMessage msg=(MessageStatusMessage)object;
    return _sessionId == msg.getSessionId() && _messageId == msg.getMessageId() && _nonce == msg.getNonce() && _size == msg.getSize() && _status == msg.getStatus();
  }
  return false;
}
