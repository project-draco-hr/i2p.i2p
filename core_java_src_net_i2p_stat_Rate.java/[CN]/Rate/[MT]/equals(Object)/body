{
  if ((obj == null) || !(obj instanceof Rate))   return false;
  if (obj == this)   return true;
  Rate r=(Rate)obj;
  return _period == r.getPeriod() && _creationDate == r.getCreationDate() && _stat == r._stat;
}
