{
  _log=context.logManager().getLog(StatManager.class);
  _context=context;
  _frequencyStats=Collections.synchronizedMap(new HashMap(8));
  _rateStats=new HashMap(128);
  if (getStatFilter() != null)   _statLog=new BufferedStatLog(context);
}
