{
  return (!ia.isLinkLocalAddress()) && (!ia.isMulticastAddress()) && (includeLoopbackAndWildcard || ((!ia.isAnyLocalAddress()) && (!ia.isLoopbackAddress())))&& (includeSiteLocal || !ia.isSiteLocalAddress())&& (includeIPv6 || (ia instanceof Inet4Address));
}
