{
  if (_cache) {
    if ((entry == null) || (entry.getData() == null))     return;
    if (shouldZero)     Arrays.fill(entry.getData(),(byte)0x0);
synchronized (_available) {
      if (_available.size() < _maxCached)       _available.add(entry);
    }
  }
}
