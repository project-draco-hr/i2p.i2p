{
  if (ALWAYS_MISS)   return false;
  if (entry == null)   throw new IllegalArgumentException("Null entry");
  if (entry.length != _entryBytes)   throw new IllegalArgumentException("Bad entry [" + entry.length + ", expected "+ _entryBytes+ "]");
synchronized (this) {
    return locked_add(entry);
  }
}
