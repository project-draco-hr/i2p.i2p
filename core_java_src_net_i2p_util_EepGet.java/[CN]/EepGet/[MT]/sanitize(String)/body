{
  name=name.replace('/','_');
  StringBuffer buf=new StringBuffer(name);
  for (int i=0; i < name.length(); i++)   if (_safeChars.indexOf(buf.charAt(i)) == -1)   buf.setCharAt(i,'_');
  return buf.toString();
}
