{
  if (!_running)   throw new IOException("Server closed for port: " + _port);
  if (_log.shouldLog(Log.DEBUG))   _log.debug("Queueing " + _port);
  try {
    _acceptQueue.put(sock);
  }
 catch (  InterruptedException ie) {
  }
}
