{
  Log rv=null;
  String scope=Log.getScope(name,cls);
synchronized (_logs) {
    rv=(Log)_logs.get(scope);
    if (rv == null) {
      rv=new Log(this,cls,name);
      _logs.put(scope,rv);
    }
  }
  updateLimit(rv);
  return rv;
}
