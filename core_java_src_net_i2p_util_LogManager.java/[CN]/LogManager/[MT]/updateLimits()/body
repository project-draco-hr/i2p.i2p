{
  Map logs=null;
synchronized (_logs) {
    logs=new HashMap(_logs);
  }
  for (Iterator iter=logs.values().iterator(); iter.hasNext(); ) {
    Log log=(Log)iter.next();
    updateLimit(log);
  }
}
