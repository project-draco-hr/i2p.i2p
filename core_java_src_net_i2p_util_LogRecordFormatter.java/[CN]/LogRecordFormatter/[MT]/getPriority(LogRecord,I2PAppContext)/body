{
  int len;
  if (Translate.getLanguage(ctx).equals("de"))   len=8;
 else   len=MAX_PRIORITY_LENGTH;
  return toString(Translate.getString(Log.toLevelString(rec.getPriority()),ctx,BUNDLE_NAME),len);
}
