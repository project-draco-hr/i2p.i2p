{
  _write=true;
  try {
    rotateFile();
    while (_write) {
      flushRecords();
      rereadConfig();
    }
  }
 catch (  Exception e) {
    System.err.println("Error writing the logs: " + e.getMessage());
    e.printStackTrace();
  }
  closeFile();
}
