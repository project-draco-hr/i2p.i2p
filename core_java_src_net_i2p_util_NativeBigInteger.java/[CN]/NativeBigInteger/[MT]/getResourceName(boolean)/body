{
  String pref=getLibraryPrefix();
  String middle=getMiddleName(optimized);
  String suff=getLibrarySuffix();
  if (pref == null || middle == null || suff == null)   return null;
  return pref + middle + "."+ suff;
}
