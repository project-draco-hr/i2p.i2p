{
  OrderedProperties prop=new OrderedProperties();
  for (int i=0; i < 100; i++)   prop.setProperty(i + "",i + " value");
  _log.debug("Thrash properties built");
  for (int i=0; i < 100; i++)   thrash(prop,i);
}
