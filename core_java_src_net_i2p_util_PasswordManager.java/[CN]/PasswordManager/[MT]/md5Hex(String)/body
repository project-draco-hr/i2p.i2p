{
  try {
    byte[] data=fullpw.getBytes("ISO-8859-1");
    byte[] sum=md5Sum(data);
    if (sum != null)     return DataHelper.toString(sum);
  }
 catch (  UnsupportedEncodingException uee) {
  }
  return null;
}
