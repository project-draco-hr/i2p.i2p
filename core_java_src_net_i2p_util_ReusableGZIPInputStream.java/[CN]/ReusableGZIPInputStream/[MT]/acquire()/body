{
  ReusableGZIPInputStream rv=null;
synchronized (_available) {
    if (_available.size() > 0)     rv=(ReusableGZIPInputStream)_available.remove(0);
  }
  if (rv == null) {
    rv=new ReusableGZIPInputStream();
  }
  return rv;
}
