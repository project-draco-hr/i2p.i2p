{
  ReusableGZIPOutputStream rv=null;
synchronized (_available) {
    if (_available.size() > 0)     rv=(ReusableGZIPOutputStream)_available.remove(0);
  }
  if (rv == null) {
    rv=new ReusableGZIPOutputStream();
  }
  return rv;
}
