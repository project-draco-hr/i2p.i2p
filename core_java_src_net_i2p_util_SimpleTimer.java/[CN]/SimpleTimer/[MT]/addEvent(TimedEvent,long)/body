{
  long eventTime=System.currentTimeMillis() + timeoutMs;
synchronized (_events) {
    while (_events.containsKey(new Long(eventTime)))     eventTime++;
    _events.put(new Long(eventTime),event);
    _events.notifyAll();
  }
}
