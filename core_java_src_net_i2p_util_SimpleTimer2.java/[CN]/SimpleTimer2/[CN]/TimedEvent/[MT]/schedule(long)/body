{
  if (_log.shouldLog(Log.DEBUG))   _log.debug("Scheduling: " + this + " timeout = "+ timeoutMs);
  if (timeoutMs <= 0 && _log.shouldLog(Log.WARN))   timeoutMs=1;
  _future=_pool.schedule(this,timeoutMs);
}
