{
  if (!ctx.isRouterContext()) {
synchronized (Translate.class) {
      if (_overrideCountry != null)       return _overrideCountry;
    }
  }
  return ctx.getProperty(PROP_COUNTRY,_localeCountry);
}
