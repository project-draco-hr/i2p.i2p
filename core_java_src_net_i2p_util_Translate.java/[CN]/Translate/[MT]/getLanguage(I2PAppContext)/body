{
  if (!ctx.isRouterContext()) {
synchronized (Translate.class) {
      if (_overrideLang != null)       return _overrideLang;
    }
  }
  String lang=ctx.getProperty(PROP_LANG);
  if (lang == null || lang.length() <= 0)   lang=_localeLang;
  return lang;
}
