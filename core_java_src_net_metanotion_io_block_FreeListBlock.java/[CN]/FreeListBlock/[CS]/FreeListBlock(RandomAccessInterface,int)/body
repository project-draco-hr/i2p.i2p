{
  this.file=file;
  this.page=startPage;
  BlockFile.pageSeek(file,startPage);
  nextPage=file.readInt();
  len=file.readInt();
  if (len > 0) {
    branches=new int[len];
    for (int i=0; i < len; i++) {
      branches[i]=file.readInt();
    }
  }
}
