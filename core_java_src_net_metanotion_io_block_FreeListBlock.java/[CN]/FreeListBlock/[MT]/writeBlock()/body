{
  BlockFile.pageSeek(file,page);
  file.writeInt(nextPage);
  if (len > 0) {
    file.writeInt(len);
    for (int i=0; i < len; i++) {
      file.writeInt(branches[i]);
    }
  }
 else {
    file.writeInt(0);
  }
}
