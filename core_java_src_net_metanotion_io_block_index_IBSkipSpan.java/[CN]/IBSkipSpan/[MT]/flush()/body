{
  super.flush();
  if (nKeys > 0)   this.firstKey=keys[0];
 else   this.firstKey=null;
  this.keys=null;
  this.vals=null;
  if (DEBUG)   System.err.println("Flushed data for page " + this.page + " containing "+ this.nKeys+ '/'+ this.spanSize);
}
