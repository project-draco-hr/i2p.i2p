{
  super.loadData();
  if (this.nKeys > 0)   this.firstKey=this.keys[0];
  if (bf.log.shouldLog(Log.DEBUG))   bf.log.debug("Loaded data for page " + this.page + " containing "+ this.nKeys+ '/'+ this.spanSize+ " first key: "+ this.firstKey);
}
