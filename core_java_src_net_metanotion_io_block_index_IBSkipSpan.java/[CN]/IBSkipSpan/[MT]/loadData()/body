{
  super.loadData();
  if (this.nKeys > 0)   this.firstKey=this.keys[0];
  if (DEBUG)   System.err.println("Loaded data for page " + this.page + " containing "+ this.nKeys+ '/'+ this.spanSize+ " first key: "+ this.firstKey);
}
