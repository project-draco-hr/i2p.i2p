{
  BSkipSpan.loadData(bss,bf,spanPage,key,val);
  if (bss.nKeys > 0)   bss.firstKey=bss.keys[0];
  if (DEBUG)   System.err.println("Loaded data for page " + spanPage + " containing "+ bss.nKeys+ '/'+ bss.spanSize+ " first key: "+ bss.firstKey);
}
