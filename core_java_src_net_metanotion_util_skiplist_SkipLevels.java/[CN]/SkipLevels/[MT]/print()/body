{
  System.out.print("SL:" + key() + "::");
  for (int i=0; i < levels.length; i++) {
    if (levels[i] != null) {
      System.out.print(i + "->" + levels[i].key()+ " ");
    }
 else {
      System.out.print(i + "->() ");
    }
  }
  System.out.print("\n");
  if (levels[0] != null) {
    levels[0].print();
  }
}
