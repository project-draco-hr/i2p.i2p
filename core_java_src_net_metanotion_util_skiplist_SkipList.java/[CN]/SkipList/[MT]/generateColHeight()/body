{
  int bits=rng.nextInt();
  boolean cont=true;
  int res=0;
  for (res=0; cont; res++) {
    cont=((bits % 2) == 0) ? true : false;
    bits=bits / 2;
  }
  return Math.max(0,Math.min(res,maxLevels()));
}
