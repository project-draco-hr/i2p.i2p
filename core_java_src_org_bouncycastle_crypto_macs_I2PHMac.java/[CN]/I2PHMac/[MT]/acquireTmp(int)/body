{
  byte rv[]=null;
synchronized (_tmpBuf[sz == 32 ? 0 : 1]) {
    if (_tmpBuf[sz == 32 ? 0 : 1].size() > 0)     rv=(byte[])_tmpBuf[sz == 32 ? 0 : 1].remove(0);
  }
  if (rv != null)   Arrays.fill(rv,(byte)0x0);
 else   rv=new byte[sz];
  return rv;
}
