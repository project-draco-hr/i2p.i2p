{
  if (args.length != 1) {
    System.err.println("Usage: FixWinPaths [wrapper.conf]\r\n");
    System.exit(1);
  }
  if (!System.getProperty("os.name").startsWith("Win")) {
    return;
  }
  replace(args[0]);
}
