{
  int length=0;
  length+=Hash.HASH_LENGTH;
  length+=4;
  length+=2;
  length+=SessionKey.KEYSIZE_BYTES;
  length+=SessionKey.KEYSIZE_BYTES;
  if (_optionsCache == null)   _optionsCache=DataHelper.toProperties(_options);
  length+=_optionsCache.length;
  length+=Hash.HASH_LENGTH;
  length+=4;
  length+=SessionTag.BYTE_LENGTH;
  length+=SessionKey.KEYSIZE_BYTES;
  length+=4;
  if (_certificateCache == null)   _certificateCache=_certificate.toByteArray();
  length+=_certificateCache.length;
  length+=DataHelper.BOOLEAN_LENGTH;
  return length;
}
