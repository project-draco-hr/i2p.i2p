{
  if (ip.length != 4)   return;
  if (add(toInt(ip)))   if (_log.shouldLog(Log.ERROR))   _log.error("Adding IP to blocklist: " + (ip[0] & 0xff) + '.'+ (ip[1] & 0xff)+ '.'+ (ip[2] & 0xff)+ '.'+ (ip[3] & 0xff));
}
