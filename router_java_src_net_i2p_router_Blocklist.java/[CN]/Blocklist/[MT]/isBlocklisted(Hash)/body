{
  List<byte[]> ips=getAddresses(peer);
  for (Iterator<byte[]> iter=ips.iterator(); iter.hasNext(); ) {
    byte ip[]=iter.next();
    if (isBlocklisted(ip)) {
      if (!_context.shitlist().isShitlisted(peer))       shitlist(peer,ip);
      return true;
    }
  }
  return false;
}
