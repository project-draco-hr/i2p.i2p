{
  List ips=getAddresses(peer);
  for (Iterator iter=ips.iterator(); iter.hasNext(); ) {
    byte ip[]=(byte[])iter.next();
    if (isBlocklisted(ip)) {
      if (!_context.shitlist().isShitlisted(peer))       shitlist(peer);
      return true;
    }
  }
  return false;
}
