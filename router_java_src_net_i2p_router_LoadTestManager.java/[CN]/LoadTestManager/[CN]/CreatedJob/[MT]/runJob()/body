{
  if (_log.shouldLog(Log.INFO))   _log.info("Tunnel created for testing peer " + _cfg.getPeer(0).toBase64());
  getContext().tunnelDispatcher().joinInbound(_cfg.getTunnel());
  _active.add(_cfg);
  Expire j=new Expire(getContext(),_cfg);
  getContext().jobQueue().addJob(j);
  runTest(_cfg);
}
