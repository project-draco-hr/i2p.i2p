{
  long timeToDiscard=_context.clock().now() - _created;
  if (_log.shouldLog(Log.DEBUG))   _log.debug("Discard " + _messageSize + "byte "+ _messageType+ " message after "+ timeToDiscard);
  _message=null;
}
