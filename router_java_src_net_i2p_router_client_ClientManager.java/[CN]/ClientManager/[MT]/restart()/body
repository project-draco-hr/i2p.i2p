{
  shutdown();
  try {
    Thread.sleep(2 * 1000);
  }
 catch (  InterruptedException ie) {
  }
  int port=_ctx.getProperty(ClientManagerFacadeImpl.PROP_CLIENT_PORT,ClientManagerFacadeImpl.DEFAULT_PORT);
  startListeners(port);
}
