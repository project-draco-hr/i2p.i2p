{
  Hash h=_runner.getDestHash(message.getSessionId());
  if (h == null)   return;
  _context.jobQueue().addJob(new LookupDestJob(_context,_runner,message.getReqID(),message.getTimeout(),message.getSessionId(),message.getHash(),message.getHostname(),h));
}
