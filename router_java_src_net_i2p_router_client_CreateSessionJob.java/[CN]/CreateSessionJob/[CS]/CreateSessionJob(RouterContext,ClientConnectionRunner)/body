{
  super(context);
  _log=context.logManager().getLog(CreateSessionJob.class);
  _runner=runner;
  if (_log.shouldLog(Log.DEBUG))   _log.debug("CreateSessionJob for runner " + _runner + " / config: "+ _runner.getConfig());
}
