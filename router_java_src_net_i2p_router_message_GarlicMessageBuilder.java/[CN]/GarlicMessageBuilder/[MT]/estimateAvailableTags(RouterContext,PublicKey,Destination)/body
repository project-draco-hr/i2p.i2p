{
  SessionKeyManager skm=ctx.sessionKeyManager();
  if (skm == null)   return 0;
  SessionKey curKey=skm.getCurrentKey(key);
  if (curKey == null)   return 0;
  return skm.getAvailableTags(key,curKey);
}
