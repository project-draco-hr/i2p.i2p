{
  boolean known=false;
synchronized (_cloves) {
    known=_cloves.containsKey(new Long(cloveId));
  }
  if (_log.shouldLog(Log.DEBUG))   _log.debug("isKnown(" + cloveId + "): "+ known);
  return known;
}
