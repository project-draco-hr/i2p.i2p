{
  Hash us=Router.getInstance().getRouterInfo().getIdentity().getHash();
  while (info != null) {
    if (us.equals(info.getThisHop()))     return info;
    info=info.getNextHopInfo();
  }
  return null;
}
