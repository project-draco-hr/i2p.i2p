{
  Hash us=Router.getInstance().getRouterInfo().getIdentity().getHash();
  TunnelInfo lastUs=null;
  while (info != null) {
    if (us.equals(info.getThisHop()))     lastUs=info;
    info=info.getNextHopInfo();
  }
  return lastUs;
}
