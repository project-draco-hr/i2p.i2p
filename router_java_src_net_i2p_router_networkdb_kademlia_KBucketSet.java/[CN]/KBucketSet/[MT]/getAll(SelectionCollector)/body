{
  long start=_context.clock().now();
  for (int i=0; i < _buckets.length; i++)   _buckets[i].getEntries(collector);
  _context.statManager().addRateData("netDb.KBSGetAllTime",_context.clock().now() - start,0);
}
