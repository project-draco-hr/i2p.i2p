{
  if (_ds == null)   return 0;
  Set<Hash> keys=_ds.getKeys();
  int rv=0;
  for (  Hash key : keys) {
    DataStructure ds=_ds.get(key);
    if (ds != null && ds instanceof LeaseSet && ((LeaseSet)ds).getReceivedAsPublished())     rv++;
  }
  return rv;
}
