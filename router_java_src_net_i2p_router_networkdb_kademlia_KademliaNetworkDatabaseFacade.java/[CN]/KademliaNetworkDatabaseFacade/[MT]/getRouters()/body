{
  if (!_initialized)   return null;
  Set routers=new HashSet();
  for (  DatabaseEntry o : getDataStore().getEntries()) {
    if (o.getType() == DatabaseEntry.KEY_TYPE_ROUTERINFO)     routers.add(o);
  }
  return routers;
}
