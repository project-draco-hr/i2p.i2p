{
synchronized (_badKeys) {
    locked_cleanupShitlist();
    boolean isShitlisted=_badKeys.contains(key);
    if (!isShitlisted)     return false;
    if (_log.shouldLog(Log.DEBUG))     _log.debug("Key " + key.toBase64() + " is shitlisted");
    return true;
  }
}
