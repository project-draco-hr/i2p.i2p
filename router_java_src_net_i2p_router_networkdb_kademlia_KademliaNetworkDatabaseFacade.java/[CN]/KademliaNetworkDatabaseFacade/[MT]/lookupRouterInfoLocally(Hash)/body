{
  if (!_initialized)   return null;
  if (_ds.isKnown(key)) {
    DataStructure ds=_ds.get(key);
    if (ds instanceof RouterInfo)     return (RouterInfo)ds;
 else {
      return null;
    }
  }
 else {
    return null;
  }
}
