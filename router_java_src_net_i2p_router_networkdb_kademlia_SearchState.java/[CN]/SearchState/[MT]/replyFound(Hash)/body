{
synchronized (_pendingPeers) {
    _pendingPeers.remove(peer);
    Long when=(Long)_pendingPeerTimes.remove(peer);
    if (when != null)     return Clock.getInstance().now() - when.longValue();
 else     return -1;
  }
}
