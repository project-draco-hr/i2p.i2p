{
  long delay=getNextRunDelay();
  if (_log.shouldLog(Log.DEBUG))   _log.debug("Updating exploration schedule with a delay of " + delay);
  getTiming().setStartAfter(_context.clock().now() + delay);
}
