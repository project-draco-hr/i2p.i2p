{
  if (_log.shouldLog(Log.INFO))   _log.info("Failed sending key " + _state.getTarget());
  if (_log.shouldLog(Log.DEBUG))   _log.debug("State of failed send: " + _state,new Exception("Who failed me?"));
  if (_onFailure != null)   _context.jobQueue().addJob(_onFailure);
}
