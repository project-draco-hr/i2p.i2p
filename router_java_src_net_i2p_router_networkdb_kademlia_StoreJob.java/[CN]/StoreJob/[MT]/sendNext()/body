{
  if (_state.completed()) {
    if (_log.shouldLog(Log.INFO))     _log.info("Already completed");
    return;
  }
  if (isExpired()) {
    _state.complete(true);
    fail();
  }
 else {
    if (_log.shouldLog(Log.INFO))     _log.info("Sending: " + _state);
    continueSending();
  }
}
