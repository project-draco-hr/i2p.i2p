{
  if (_state.completed()) {
    _log.info("Already completed");
    return;
  }
  if (isExpired()) {
    _state.complete(true);
    fail();
  }
 else {
    _log.info("Sending: " + _state);
    continueSending();
  }
}
