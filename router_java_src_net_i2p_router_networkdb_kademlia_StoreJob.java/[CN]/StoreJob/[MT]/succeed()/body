{
  if (_log.shouldLog(Log.INFO))   _log.info("Succeeded sending key " + _state.getTarget());
  if (_log.shouldLog(Log.DEBUG))   _log.debug("State of successful send: " + _state);
  if (_onSuccess != null)   JobQueue.getInstance().addJob(_onSuccess);
  _facade.noteKeySent(_state.getTarget());
}
