{
  RouterContext ctx=new RouterContext(new net.i2p.router.Router());
  testProfileSize(ctx,100,0);
  testProfileSize(ctx,1000,0);
  testProfileSize(ctx,10000,0);
  testProfileSize(ctx,0,10000);
  testProfileSize(ctx,0,100000);
  testProfileSize(ctx,0,300000);
}
