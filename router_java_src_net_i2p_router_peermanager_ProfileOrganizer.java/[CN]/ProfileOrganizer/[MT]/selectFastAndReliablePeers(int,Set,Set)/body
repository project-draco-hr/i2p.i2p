{
synchronized (_reorganizeLock) {
    locked_selectPeers(_fastAndReliablePeers,howMany,exclude,matches);
  }
  if (matches.size() < howMany)   selectReliablePeers(howMany,exclude,matches);
  return;
}
