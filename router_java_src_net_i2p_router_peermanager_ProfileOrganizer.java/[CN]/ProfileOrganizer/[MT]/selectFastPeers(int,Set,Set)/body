{
synchronized (_reorganizeLock) {
    locked_selectPeers(_fastPeers,howMany,exclude,matches);
  }
  if (matches.size() < howMany) {
    if (_log.shouldLog(Log.DEBUG))     _log.debug("selectFastPeers(" + howMany + "), not enough fast ("+ matches.size()+ ") going on to highCap");
    selectHighCapacityPeers(howMany,exclude,matches);
  }
 else {
    if (_log.shouldLog(Log.DEBUG))     _log.debug("selectFastPeers(" + howMany + "), found enough fast ("+ matches.size()+ ")");
  }
  return;
}
