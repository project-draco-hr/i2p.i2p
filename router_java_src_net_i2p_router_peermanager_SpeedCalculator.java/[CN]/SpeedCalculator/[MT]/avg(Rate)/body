{
  long events=rate.getCurrentEventCount() + rate.getLastEventCount();
  long time=rate.getCurrentTotalEventTime() + rate.getLastTotalEventTime();
  if ((events > 0) && (time > 0))   return time / events;
 else   return 0.0d;
}
