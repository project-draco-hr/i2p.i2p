{
  SigType cstype=CreateRouterInfoJob.DEFAULT_SIGTYPE;
  String sstype=ctx.getProperty(PROP_ROUTER_SIGTYPE);
  if (sstype != null) {
    SigType ntype=SigType.parseSigType(sstype);
    if (ntype != null && ntype.isAvailable())     cstype=ntype;
  }
  return cstype;
}
