{
  if (!_wasChecked) {
    try {
      LoadClientAppsJob.testClient(NEW_CLASS,null);
      _hasJetty6=true;
    }
 catch (    ClassNotFoundException cnfe) {
    }
    _wasChecked=true;
  }
  return _hasJetty6;
}
