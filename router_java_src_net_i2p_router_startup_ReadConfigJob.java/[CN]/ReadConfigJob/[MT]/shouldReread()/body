{
  File configFile=new File(getContext().router().getConfigFilename());
  if (!configFile.exists())   return false;
  if (configFile.lastModified() > _lastRead)   return true;
 else   return false;
}
