{
  if (!_clients.containsKey(app))   return false;
  if (_log.shouldLog(Log.INFO))   _log.info("Client " + app.getDisplayName() + " REGISTERED AS "+ app.getName());
  return _registered.putIfAbsent(app.getName(),app) == null;
}
