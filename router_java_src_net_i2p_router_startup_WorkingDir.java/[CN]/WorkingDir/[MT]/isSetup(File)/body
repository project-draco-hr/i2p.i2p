{
  if (dir.isDirectory()) {
    String[] files=dir.list();
    if (files == null)     return false;
    for (    String file : files)     if (!"plugins".equals(file) && !"logs".equals(file))     return true;
  }
  return false;
}
