{
  if (dir.isDirectory()) {
    String[] files=dir.list();
    if (files.length == 0)     return false;
    if (files.length > 1 || !"plugins".equals(files[0]))     return true;
  }
  return false;
}
