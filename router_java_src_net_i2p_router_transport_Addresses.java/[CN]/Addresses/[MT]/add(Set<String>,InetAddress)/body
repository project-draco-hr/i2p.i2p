{
  if (ia.isAnyLocalAddress() || ia.isLinkLocalAddress() || ia.isLoopbackAddress()|| ia.isMulticastAddress()|| ia.isSiteLocalAddress()|| ia.getHostAddress().startsWith("5.")|| !(ia instanceof Inet4Address)) {
    return;
  }
  String ip=ia.getHostAddress();
  set.add(ip);
}
