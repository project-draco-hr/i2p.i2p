{
  FIFOBandwidthLimiter.Request req=null;
  if (_pullFromOutbound)   req=_context.bandwidthLimiter().requestOutbound(1,_peerSource);
 else   req=_context.bandwidthLimiter().requestInbound(1,_peerSource);
  req.waitForNextAllocation();
  return in.read();
}
