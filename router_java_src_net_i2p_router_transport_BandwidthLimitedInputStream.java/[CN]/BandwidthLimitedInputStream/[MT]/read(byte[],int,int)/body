{
  int read=in.read(dest,off,len);
  if (_pullFromOutbound)   _context.bandwidthLimiter().requestOutbound(read,_peerSource);
 else   _context.bandwidthLimiter().requestInbound(read,_peerSource);
  return read;
}
