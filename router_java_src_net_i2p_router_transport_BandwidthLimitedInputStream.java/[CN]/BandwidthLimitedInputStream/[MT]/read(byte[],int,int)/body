{
  int read=in.read(dest,off,len);
  if (_pullFromOutbound)   _context.bandwidthLimiter().delayOutbound(_peer,read,true);
 else   _context.bandwidthLimiter().delayInbound(_peer,read);
  return read;
}
