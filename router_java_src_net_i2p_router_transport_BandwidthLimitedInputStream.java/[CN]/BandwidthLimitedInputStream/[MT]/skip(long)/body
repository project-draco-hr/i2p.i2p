{
  long skip=in.skip(numBytes);
  if (_pullFromOutbound)   _context.bandwidthLimiter().delayOutbound(_peer,(int)skip);
 else   _context.bandwidthLimiter().delayInbound(_peer,(int)skip);
  return skip;
}
