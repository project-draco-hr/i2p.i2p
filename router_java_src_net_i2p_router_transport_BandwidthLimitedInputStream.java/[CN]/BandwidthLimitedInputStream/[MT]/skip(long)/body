{
  long skip=in.skip(numBytes);
  if (_pullFromOutbound)   _context.bandwidthLimiter().requestOutbound((int)skip,_peerSource);
 else   _context.bandwidthLimiter().requestInbound((int)skip,_peerSource);
  return skip;
}
