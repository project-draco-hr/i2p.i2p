{
  List<RouterAddress> addresses=new ArrayList(_manager.getAddresses());
  if (_log.shouldLog(Log.INFO))   _log.info("Creating addresses: " + addresses,new Exception("creator"));
  return addresses;
}
