{
  _inAllocated+=in;
  _outAllocated+=out;
  if (_lsnr == null)   _allocationsSinceWait++;
  if (isComplete()) {
    if (_log.shouldLog(Log.INFO))     _log.info("allocate " + in + "/"+ out+ " completed, listener="+ _lsnr);
  }
}
