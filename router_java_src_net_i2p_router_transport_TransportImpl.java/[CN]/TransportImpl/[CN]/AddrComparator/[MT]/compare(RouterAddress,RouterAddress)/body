{
  int lc=l.getCost();
  int rc=r.getCost();
  byte[] lip=l.getIP();
  byte[] rip=r.getIP();
  if (lip != null && lip.length == 16)   lc+=adj;
  if (rip != null && rip.length == 16)   rc+=adj;
  if (lc > rc)   return 1;
  if (lc < rc)   return -1;
  return 0;
}
