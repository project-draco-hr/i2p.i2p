{
  if (_wasUnreachableEntries.contains(peer))   return true;
  RouterInfo ri=_context.netDb().lookupRouterInfoLocally(peer);
  if (ri == null)   return false;
  return null == ri.getTargetAddress(this.getStyle());
}
