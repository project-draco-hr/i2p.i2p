{
  if (_transports.size() <= 0)   return false;
  for (int i=0; i < _transports.size(); i++) {
    if (!((Transport)_transports.get(i)).haveCapacity(HIGH_CAPACITY_PCT))     return false;
  }
  return true;
}
