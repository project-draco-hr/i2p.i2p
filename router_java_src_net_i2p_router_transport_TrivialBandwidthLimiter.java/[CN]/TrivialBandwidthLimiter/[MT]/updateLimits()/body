{
  if (_log.shouldLog(Log.DEBUG))   _log.debug("Updating rates for the bw limiter");
  _lastUpdateLimits=_context.clock().now();
  updateInboundRate();
  updateOutboundRate();
  updateInboundPeak();
  updateOutboundPeak();
  updateReplenishFrequency();
  updateMinNonZeroDelay();
}
