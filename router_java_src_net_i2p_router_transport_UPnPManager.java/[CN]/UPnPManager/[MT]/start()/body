{
  if (_log.shouldLog(Log.DEBUG)) {
    _log.debug("UPnP Start");
    Debug.on();
  }
  if (!_isRunning)   _isRunning=_upnp.runPlugin();
  if (!_isRunning)   _log.error("UPnP start failed - port conflict?");
}
