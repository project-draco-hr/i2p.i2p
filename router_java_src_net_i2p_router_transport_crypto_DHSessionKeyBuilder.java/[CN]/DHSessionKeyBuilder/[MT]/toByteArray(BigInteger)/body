{
  byte data[]=bi.toByteArray();
  byte rv[]=new byte[256];
  if (data.length == 257)   System.arraycopy(data,1,rv,0,rv.length);
 else   if (data.length == 256)   System.arraycopy(data,0,rv,0,rv.length);
 else   System.arraycopy(data,0,rv,rv.length - data.length,data.length);
  return rv;
}
