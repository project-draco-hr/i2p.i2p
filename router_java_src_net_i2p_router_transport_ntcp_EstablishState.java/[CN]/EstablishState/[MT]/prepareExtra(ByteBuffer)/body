{
  _extra=new byte[buf.remaining()];
  buf.get(_extra);
  _received+=_extra.length;
  if (_log.shouldLog(Log.DEBUG))   _log.debug(prefix() + "prepare extra " + _extra.length+ " (total received: "+ _received+ ")");
}
