{
  I2NPMessageHandler rv=null;
synchronized (_i2npHandlers) {
    if (_i2npHandlers.size() > 0)     rv=(I2NPMessageHandler)_i2npHandlers.remove(0);
  }
  if (rv == null)   rv=new I2NPMessageHandler(ctx);
  return rv;
}
