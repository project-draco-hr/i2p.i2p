{
  return "NTCP conn " + (_isInbound ? "from " : "to ") + (_remotePeer == null ? "unknown" : _remotePeer.calculateHash().toBase64().substring(0,6))+ (_established ? "" : " not established")+ " created "+ DataHelper.formatDuration(getTimeSinceCreated())+ " ago";
}
