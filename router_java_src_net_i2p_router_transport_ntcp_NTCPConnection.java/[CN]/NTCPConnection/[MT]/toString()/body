{
  return "NTCP Connection to " + (_remotePeer == null ? "unknown " : _remotePeer.calculateHash().toBase64().substring(0,6)) + " inbound? "+ _isInbound+ " established? "+ _established+ " created "+ DataHelper.formatDuration(getTimeSinceCreated())+ " ago";
}
