{
  int active=0;
  for (  NTCPConnection con : _conByIdent.values()) {
    if ((con.getTimeSinceSend() <= 60 * 1000) || (con.getTimeSinceReceive() <= 60 * 1000))     active++;
  }
  return active;
}
