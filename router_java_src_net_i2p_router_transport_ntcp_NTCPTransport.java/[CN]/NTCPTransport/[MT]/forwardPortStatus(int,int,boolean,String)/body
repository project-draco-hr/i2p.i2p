{
  if (_log.shouldLog(Log.WARN)) {
    if (success)     _log.warn("UPnP has opened the NTCP port: " + port);
 else     _log.warn("UPnP has failed to open the NTCP port: " + port + " reason: "+ reason);
  }
}
