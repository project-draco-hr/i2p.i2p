{
  if (isAlive() && getCurrentAddress(false) != null) {
    for (    NTCPConnection con : _conByIdent.values()) {
      if (con.isInbound())       return CommSystemFacade.STATUS_OK;
    }
  }
  return CommSystemFacade.STATUS_UNKNOWN;
}
