{
  if (_pumper.isAlive())   return;
  if (_log.shouldLog(Log.WARN))   _log.warn("Restarting ntcp transport listening");
  startIt();
  if (addr == null)   _myAddress=null;
 else   _myAddress=new NTCPAddress(addr);
  bindAddress();
}
