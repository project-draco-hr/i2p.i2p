{
  SimpleTimer.getInstance().addEvent(new DieIfTooSlow(),CONNECTION_TIMEOUT);
  try {
    return doEstablishConnection();
  }
 catch (  Exception e) {
    if (_socket != null)     fail("Error connecting",e);
    return null;
  }
}
