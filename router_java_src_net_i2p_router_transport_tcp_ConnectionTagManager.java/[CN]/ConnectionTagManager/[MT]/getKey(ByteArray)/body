{
synchronized (_lock) {
    for (Iterator iter=_tags.keySet().iterator(); iter.hasNext(); ) {
      Hash peer=(Hash)iter.next();
      ByteArray cur=(ByteArray)_tags.get(peer);
      if (cur.equals(tag))       return (SessionKey)_keys.get(peer);
    }
    return null;
  }
}
