{
  RouterAddress addr=toAddress.getTargetAddress(STYLE);
  if (addr == null)   return null;
  if ((_myAddress != null) && (_myAddress.equals(addr)))   return null;
  if (getIsConnected(toAddress.getIdentity()))   return _fastBid;
 else   return _slowBid;
}
