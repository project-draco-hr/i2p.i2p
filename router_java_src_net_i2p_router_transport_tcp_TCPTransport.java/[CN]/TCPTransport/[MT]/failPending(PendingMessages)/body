{
  if (pending != null) {
synchronized (_msgs) {
      _msgs.remove(pending.getPeer());
    }
    OutNetMessage msg=null;
    while ((msg=pending.getNextMessage()) != null) {
      afterSend(msg,false);
    }
  }
}
