{
synchronized (_connections) {
    if (!_connections.containsKey(peer))     return null;
    List cons=(List)_connections.get(peer);
    if (cons.size() <= 0)     return null;
    TCPConnection first=(TCPConnection)cons.get(0);
    return first;
  }
}
