{
  long now=_context.clock().now();
  state.prepareSessionRequest();
  if (_log.shouldLog(Log.DEBUG))   _log.debug("Send request to: " + state.getRemoteHostInfo());
  _transport.send(_builder.buildSessionRequestPacket(state));
  state.requestSent();
}
