{
  while (_alive) {
    try {
synchronized (_stateLock) {
        if (_completeMessages.size() > 0)         return (InboundMessageState)_completeMessages.remove(0);
        _stateLock.wait();
      }
    }
 catch (    InterruptedException ie) {
    }
  }
  return null;
}
