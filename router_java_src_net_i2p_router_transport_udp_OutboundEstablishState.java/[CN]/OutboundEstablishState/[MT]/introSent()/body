{
  _lastSend=_context.clock().now();
  long delay=Math.min(RETRANSMIT_DELAY << (_introSentCount++),MAX_DELAY);
  _nextSend=_lastSend + delay;
  if (_currentState == STATE_UNKNOWN)   _currentState=STATE_PENDING_INTRO;
}
