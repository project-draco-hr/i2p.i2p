{
  if (_dead)   return;
  _dead=true;
  Map msgs=_outboundMessages;
  _retransmitter=null;
  if (msgs != null) {
synchronized (msgs) {
      for (Iterator iter=msgs.values().iterator(); iter.hasNext(); )       _transport.failed((OutboundMessageState)iter.next());
      msgs.clear();
    }
  }
}
