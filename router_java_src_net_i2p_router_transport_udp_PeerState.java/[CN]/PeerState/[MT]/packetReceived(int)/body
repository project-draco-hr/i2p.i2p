{
  _packetsReceived++;
  size+=OVERHEAD_SIZE;
  if (size <= MIN_MTU) {
    _consecutiveSmall++;
    if (_consecutiveSmall >= MTU_RCV_DISPLAY_THRESHOLD)     _mtuReceive=MIN_MTU;
  }
 else {
    _consecutiveSmall=0;
    if (size > _mtuReceive)     _mtuReceive=size;
  }
}
