{
  int rv=0;
  for (int i=0; _ip != null && i < _ip.length; i++)   rv+=_ip[i] << i;
  for (int i=0; _peerHash != null && i < _peerHash.length; i++)   rv+=_peerHash[i] << i;
  rv+=_port;
  return rv;
}
