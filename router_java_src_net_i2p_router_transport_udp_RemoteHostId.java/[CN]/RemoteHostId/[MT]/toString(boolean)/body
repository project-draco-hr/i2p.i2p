{
  StringBuffer buf=new StringBuffer(_ip.length + 5);
  for (int i=0; i < _ip.length; i++) {
    buf.append(_ip[i] & 0xFF);
    if (i + 1 < _ip.length)     buf.append('.');
  }
  if (includePort)   buf.append(':').append(_port);
  return buf.toString();
}
