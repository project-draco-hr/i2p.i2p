{
  _dataCache.release(_dataBuf);
  if (!CACHE)   return;
synchronized (_packetCache) {
    _packet.setLength(0);
    _packet.setPort(1);
    if (_packetCache.size() <= 64)     _packetCache.add(this);
  }
}
