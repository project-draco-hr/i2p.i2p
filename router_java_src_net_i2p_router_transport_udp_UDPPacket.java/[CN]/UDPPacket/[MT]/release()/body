{
  verifyNotReleased();
  _released=true;
  if (!CACHE) {
    _dataCache.release(_dataBuf);
    return;
  }
synchronized (_packetCache) {
    if (_packetCache.size() <= 64) {
      _packetCache.add(this);
    }
 else {
      _dataCache.release(_dataBuf);
    }
  }
}
