{
  int count=fragmentCount();
  int rv=-1;
  for (int i=0; i < _bitfieldSize; i++) {
    byte b=_message[_bitfieldStart + i];
    b&=0x7f;
    int j=0;
    while (b != 0 && j++ < 7) {
      if ((b & 0x01) != 0)       rv=(7 * i) + j;
      b>>=1;
      b&=0x7f;
    }
  }
  return rv;
}
