{
  StringBuffer buf=new StringBuffer(64);
  buf.append("Read partial ACK of ");
  buf.append(getMessageId());
  buf.append(" with ACKs for: ");
  int numFrags=fragmentCount();
  for (int i=0; i < numFrags; i++)   if (received(i))   buf.append(i).append(" ");
  return buf.toString();
}
