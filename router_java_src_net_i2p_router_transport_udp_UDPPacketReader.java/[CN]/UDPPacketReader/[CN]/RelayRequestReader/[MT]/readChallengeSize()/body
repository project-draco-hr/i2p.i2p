{
  int offset=readBodyOffset() + 4;
  offset+=DataHelper.fromLong(_message,offset,1);
  offset++;
  offset+=2;
  int rv=(int)DataHelper.fromLong(_message,offset,1);
  if (_log.shouldLog(Log.WARN))   _log.warn("read challenge size: " + rv);
  return rv;
}
