{
  int offset=readBodyOffset() + 4;
  offset+=DataHelper.fromLong(_message,offset,1);
  offset++;
  int rv=(int)DataHelper.fromLong(_message,offset,2);
  if (_log.shouldLog(Log.WARN))   _log.warn("read alice port: " + rv);
  return rv;
}
