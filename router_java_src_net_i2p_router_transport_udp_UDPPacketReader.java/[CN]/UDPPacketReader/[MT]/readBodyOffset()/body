{
  int offset=_payloadBeginOffset + 1 + 4;
  if (readRekeying())   offset+=KEYING_MATERIAL_LENGTH;
  if (readExtendedOptionsIncluded()) {
    int optionsSize=(int)DataHelper.fromLong(_message,offset,1);
    offset+=optionsSize + 1;
  }
  return offset;
}
