{
  if ((_externalListenPort > 0) && (_externalListenHost != null))   return;
  RelayPeer peer=new RelayPeer(host,port,tag,relayIntroKey);
synchronized (_relayPeers) {
    _relayPeers.add(peer);
  }
}
