{
  long now=_context.clock().now();
  int active=0;
  for (  PeerState peer : _peersByIdent.values()) {
    if (now - peer.getLastSendFullyTime() <= 1 * 60 * 1000)     active++;
  }
  return active;
}
