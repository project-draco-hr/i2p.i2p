{
  if (_log.shouldLog(Log.WARN)) {
    if (success)     _log.warn("UPnP has opened the SSU port: " + port + " via external port: "+ externalPort);
 else     _log.warn("UPnP has failed to open the SSU port: " + port + " reason: "+ reason);
  }
  if (success && getExternalIP() != null)   setReachabilityStatus(CommSystemFacade.STATUS_OK);
}
