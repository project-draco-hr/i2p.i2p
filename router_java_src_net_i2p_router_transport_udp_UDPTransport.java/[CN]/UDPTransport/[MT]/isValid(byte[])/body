{
  if (addr == null)   return false;
  if (addr.length < 4)   return false;
  if (isPubliclyRoutable(addr))   return true;
  return Boolean.valueOf(_context.getProperty("i2np.udp.allowLocal")).booleanValue();
}
