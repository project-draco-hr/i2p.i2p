{
  final PeerState ps=_peersByIdent.get(peer);
  if (ps != null && ps.isInbound() && ps.getWeRelayToThemAs() <= 0 && ps.getMessagesReceived() <= 2 && ps.getMessagesSent() <= 2) {
    ps.setMayDisconnect();
  }
}
