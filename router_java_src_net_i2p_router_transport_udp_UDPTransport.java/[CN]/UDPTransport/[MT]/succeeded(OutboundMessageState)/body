{
  if (msg == null)   return;
  if (_log.shouldLog(Log.DEBUG))   _log.debug("Sending message succeeded: " + msg);
  noteSend(msg,true);
  if (msg.getMessage() != null)   super.afterSend(msg.getMessage(),true);
}
