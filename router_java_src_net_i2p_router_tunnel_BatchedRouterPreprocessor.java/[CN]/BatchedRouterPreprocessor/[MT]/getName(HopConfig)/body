{
  if (cfg == null)   return "[unknown]";
  if (cfg.getReceiveTunnel() != null)   return cfg.getReceiveTunnel().getTunnelId() + "";
 else   if (cfg.getSendTunnel() != null)   return cfg.getSendTunnel().getTunnelId() + "";
 else   return "[n/a]";
}
