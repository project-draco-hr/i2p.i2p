{
  int numHops=8;
  TunnelCreatorConfig config=prepareConfig(numHops);
  long start=_context.clock().now();
  for (int i=0; i < 1; i++)   runTest(numHops,config);
  long time=_context.clock().now() - start;
  _log.debug("Time for 1000 messages: " + time);
}
