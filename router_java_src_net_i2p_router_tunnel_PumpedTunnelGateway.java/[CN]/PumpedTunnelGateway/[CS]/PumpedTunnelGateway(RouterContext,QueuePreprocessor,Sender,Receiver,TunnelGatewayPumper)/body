{
  super(context,preprocessor,sender,receiver);
  if (getClass() == PumpedTunnelGateway.class)   _prequeue=new LinkedBlockingQueue(MAX_OB_QUEUE);
 else   _prequeue=new LinkedBlockingQueue(MAX_IB_QUEUE);
  _pumper=pumper;
}
