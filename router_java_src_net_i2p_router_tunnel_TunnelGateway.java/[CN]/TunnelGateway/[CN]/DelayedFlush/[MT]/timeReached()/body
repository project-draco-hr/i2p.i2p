{
  boolean wantRequeue=false;
synchronized (_queue) {
    if (_queue.size() > 0)     wantRequeue=_preprocessor.preprocessQueue(_queue,_sender,_receiver);
  }
  if (wantRequeue)   SimpleTimer.getInstance().addEvent(_delayedFlush,_flushFrequency);
 else   _lastFlush=_context.clock().now();
}
