{
  if (_log.shouldLog(Log.DEBUG))   _log.debug("Request " + _state.msg.getUniqueId() + " handled with a successful deferred lookup for the next peer "+ _nextPeer.toBase64());
  handleReq(getContext().netDb().lookupRouterInfoLocally(_nextPeer),_state,_req,_nextPeer);
}
