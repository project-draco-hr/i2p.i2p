{
  if (_testJob != null)   _testJob.testSuccessful(ms);
  super.testSuccessful(ms);
  if ((_context.router().getUptime() < 10 * 60 * 1000) && (!_live))   setExpiration(getExpiration() - _context.random().nextInt(5 * 60 * 1000));
  _live=true;
}
