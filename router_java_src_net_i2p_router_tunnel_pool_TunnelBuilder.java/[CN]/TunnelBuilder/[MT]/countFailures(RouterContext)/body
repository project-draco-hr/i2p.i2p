{
  RateStat rs=ctx.statManager().getRate("tunnel.testFailedTime");
  if (rs == null)   return 0;
  Rate r=rs.getRate(10 * 60 * 1000);
  if (r == null)   return 0;
 else   return r.getCurrentEventCount();
}
