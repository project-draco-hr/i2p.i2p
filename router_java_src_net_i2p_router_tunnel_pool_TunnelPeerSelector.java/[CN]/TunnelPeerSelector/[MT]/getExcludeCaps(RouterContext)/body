{
  String excludeCaps=ctx.getProperty("router.excludePeerCaps",String.valueOf(Router.CAPABILITY_BW12));
  Set peers=new HashSet();
  if (excludeCaps != null) {
    char excl[]=excludeCaps.toCharArray();
    return excl;
  }
 else {
    return null;
  }
}
