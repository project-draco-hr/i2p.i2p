{
  if (_pool == null) {
    _pool=new TunnelPool(_context);
    if (_log.shouldLog(Log.DEBUG))     _log.debug(toString() + ": New tunnel pool created: " + _pool.toString());
  }
  _pool.startup();
  _testManager=new TunnelTestManager(_context,_pool);
}
