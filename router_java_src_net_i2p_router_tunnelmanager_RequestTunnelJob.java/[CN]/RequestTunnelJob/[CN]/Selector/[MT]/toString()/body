{
  return "Build Tunnel Job Selector for tunnel " + _tunnel.getTunnelId().getTunnelId() + " at "+ _tunnel.getThisHop().toBase64()+ " [found="+ _statusFound+ ", ack="+ _ackFound+ "] (@"+ (new Date(getExpiration()))+ ")";
}
