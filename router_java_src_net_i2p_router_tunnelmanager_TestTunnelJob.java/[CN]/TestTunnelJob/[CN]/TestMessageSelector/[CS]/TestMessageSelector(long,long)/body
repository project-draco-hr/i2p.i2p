{
  _id=id;
  _tunnelId=tunnelId;
  _found=false;
  _expiration=getContext().clock().now() + TEST_TIMEOUT;
  if (_log.shouldLog(Log.DEBUG))   _log.debug("the expiration while testing tunnel " + tunnelId + " waiting for nonce "+ id+ ": "+ new Date(_expiration));
}
