{
  if (!_found) {
    if (_log.shouldLog(Log.DEBUG))     _log.debug("Continue matching while looking for nonce for tunnel " + _tunnelId);
  }
 else {
    if (_log.shouldLog(Log.INFO))     _log.info("Don't continue matching for tunnel " + _tunnelId + " / "+ _id);
  }
  return !_found;
}
