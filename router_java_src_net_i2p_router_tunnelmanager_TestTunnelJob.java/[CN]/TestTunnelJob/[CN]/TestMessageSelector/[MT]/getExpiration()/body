{
  if (_expiration < Clock.getInstance().now()) {
    if (_log.shouldLog(Log.DEBUG))     _log.debug("EXPIRED while looking for nonce " + _id + " for tunnel "+ _tunnelId);
  }
  return _expiration;
}
