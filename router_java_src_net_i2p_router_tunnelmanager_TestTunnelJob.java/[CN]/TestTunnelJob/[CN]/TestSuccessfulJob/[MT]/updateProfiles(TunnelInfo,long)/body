{
  TunnelInfo cur=info;
  while (cur != null) {
    Hash peer=cur.getThisHop();
    if ((peer != null) && (!_context.routerHash().equals(peer)))     _context.profileManager().tunnelTestSucceeded(peer,time);
    cur=cur.getNextHopInfo();
  }
}
