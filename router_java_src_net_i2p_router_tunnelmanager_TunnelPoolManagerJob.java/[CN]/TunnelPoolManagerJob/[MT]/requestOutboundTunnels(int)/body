{
  _log.info("Requesting " + numTunnelsToRequest + " outbound tunnels");
  for (int i=0; i < numTunnelsToRequest; i++)   getContext().jobQueue().addJob(new RequestOutboundTunnelJob(getContext(),_pool,false));
}
