{
  String filename=null;
  String str=_context.router().getConfigSetting(PARAM_TUNNEL_POOL_FILE);
  if ((str != null) && (str.trim().length() > 0))   filename=str;
 else   filename=DEFAULT_TUNNEL_POOL_FILE;
  return new File(filename);
}
