{
  if (serverSock == null)   return null;
  try {
    Socket sock=serverSock.accept();
    sock.setSoTimeout(HTTP.DEFAULT_PORT * 1000);
    return sock;
  }
 catch (  Exception e) {
    return null;
  }
}
