{
  String xmlStr=toString();
  xmlStr=xmlStr.replaceAll("<","&lt;");
  xmlStr=xmlStr.replaceAll(">","&gt;");
  xmlStr=xmlStr.replaceAll("&","&amp;");
  xmlStr=xmlStr.replaceAll("\"","&quot;");
  xmlStr=xmlStr.replaceAll("'","&apos;");
  return xmlStr;
}
